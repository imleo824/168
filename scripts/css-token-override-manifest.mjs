// Root-level design tokens have a single owner. Keep this manifest empty;
// scoped runtime overrides belong on a page or component data contract.
export const allowedRootTokenOverrideManifests = [];

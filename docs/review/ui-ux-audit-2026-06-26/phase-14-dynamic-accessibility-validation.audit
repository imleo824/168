# UI/UE 深度审计 Phase 14 - 动态验证与可达性执行表

日期: 2026-06-26

目标: 将 Phase 9 中的动态验证协议展开成可执行检查表。截图可以证明静态视觉，但不能证明键盘、focus、弹层、移动键盘、极端文本、坏图、对比度和动效质量。Phase 14 用于后续逐项回填证据，避免把“看起来精致”误判成“体验真的稳定”。

本阶段状态: 执行表已建立，动态验证尚未实际运行。不能把本文件当作通过证明。

## 执行总原则

1. 每个动态验证必须绑定 route、viewport、user state、data state、interaction、expected result。
2. 每个失败必须记录 severity、复现步骤、截图或录像证据、影响范围。
3. 验证必须覆盖 mobile 390 x 844、desktop 1280 x 720。
4. 涉及键盘和底部操作的流程必须补 compact mobile 360 x 740。
5. 涉及 desktop composition 的流程建议补 wide 1440 x 900。
6. 截图不能替代键盘和 focus 记录。
7. 通过标准不能写“感觉顺滑”，必须写具体可观察行为。
8. 如果使用浏览器执行，优先使用 Codex in-app Browser；如果无法执行，再按 Product Design 审计流程选择 fallback。

## 证据目录建议

动态验证记录建议保存到:

`docs/review/ui-ux-audit-2026-06-26/dynamic-validation/`

命名格式:

`dv{number}-{viewport}-{route-or-flow}-{state}.audit`

示例:

- `dv01-mobile-create-tab-order.audit`
- `dv02-desktop-profile-dialog-focus.audit`
- `dv03-compact-promote-payment-keyboard.audit`
- `dv06-desktop-contrast-muted-text.audit`

如果有截图或录像，继续放入:

`docs/review/ui-ux-audit-2026-06-26/screenshots/`

## 记录模板

每个动态验证记录必须包含:

```text
Check ID:
Route:
Viewport:
User state:
Data state:
Interaction:
Expected result:
Actual result:
Accepted: yes/no
Severity if failed:
Evidence:
Accessibility impact:
Design impact:
Engineering follow-up:
Completion verdict:
```

`Completion verdict` 只能使用:

- `proved`
- `partially proved`
- `not proved`

## DV01: Keyboard tab order

目标: 验证键盘路径是否符合视觉顺序，保证产品不是只能用鼠标或触屏操作。

覆盖范围:

- `/`
- `/create`
- `/post/:id`
- `/profile`
- `/promote`
- `/recharge`
- `/transactions`
- `/sponsor`
- `/category/:categoryId`
- `/about`
- `/404`

优先级:

- P0: `/`、`/create`、`/post/:id`、`/profile`、`/promote`
- P1: `/recharge`、`/transactions`、`/sponsor`、`/category/:categoryId`
- P2: `/about`、`/404`、`/168wc`

执行步骤:

1. 从页面顶部开始按 Tab。
2. 记录每次 focus 到达的元素。
3. 继续到页面主要 action、输入、tab、sheet trigger、底部导航。
4. 用 Shift + Tab 反向检查一次。
5. 对 icon-only action 检查是否有可理解 label。

通过标准:

- Tab 顺序和视觉顺序一致。
- Focus ring 清楚可见。
- Focus 不被 `overflow`、sticky header、bottom bar 裁切。
- Icon-only action 有可理解 label。
- Disabled 元素不被错误聚焦。

失败分级:

- P0: 关键 action 无法键盘触达。
- P0: Focus 被裁切到不可见。
- P1: Tab 顺序跳跃但仍可完成任务。
- P2: Focus ring 可见但视觉不够精致。

## DV02: Dialog, sheet, lightbox focus and Escape

目标: 验证弹层类组件是否有稳定焦点策略、关闭方式和背景滚动控制。

覆盖组件:

- `BottomSheet`
- `HomeStructuredFilterSheet`
- `PromotePaymentSheet`
- `ProfileDialog`
- `ImageLightbox`
- Post quote sheet
- Telegram sync confirm sheet

覆盖页面:

- `/`
- `/create`
- `/profile`
- `/post/:id`
- `/promote`

执行步骤:

1. 使用键盘或触发按钮打开弹层。
2. 记录打开后 focus 位置。
3. 连续按 Tab，确认焦点是否留在弹层内或有明确策略。
4. 按 Escape。
5. 确认弹层关闭。
6. 确认 focus 回到触发点或合理位置。
7. 在弹层打开时尝试滚动背景。

通过标准:

- 弹层打开后 focus 进入可操作区域。
- Escape 可关闭可关闭弹层。
- 关闭后 focus 恢复合理。
- 背景不误滚。
- Dialog 有可理解标题或 label。
- Sheet header、body、footer 不在键盘或 safe area 下互相遮挡。

失败分级:

- P0: 支付、发布、资料编辑弹层无法关闭。
- P0: Focus 跑到背景且用户无法回到弹层。
- P1: Escape 无效但有清楚关闭按钮。
- P1: 背景可滚导致上下文丢失。
- P2: Focus 恢复位置不理想但不阻塞任务。

## DV03: Mobile keyboard and safe area

目标: 验证移动键盘弹起时输入框、底部 action、sheet footer 和导航不会互相遮挡。

覆盖流程:

- 发布文本输入。
- 发布分类字段输入。
- 发布地点搜索。
- Profile bio 编辑。
- Promote payment password。
- Recharge amount。
- Chat message input。

视口:

- mobile 390 x 844。
- compact mobile 360 x 740。

执行步骤:

1. 聚焦输入框。
2. 等待移动键盘弹起。
3. 输入短文本。
4. 输入长文本。
5. 尝试点击底部主 action。
6. 滚动到输入框上下区域。
7. 收起键盘后确认页面位置恢复合理。

通过标准:

- 当前输入框不被键盘遮挡。
- 主 action 不被键盘或底部导航遮挡。
- Sheet footer 不压住输入内容。
- 页面不会跳到不可恢复位置。
- 输入多行文本时行高和滚动稳定。

失败分级:

- P0: 输入框或提交按钮被键盘遮挡，无法完成任务。
- P0: 键盘弹起后页面跳动导致用户迷失。
- P1: 可完成但底部 action 与键盘距离过近。
- P2: 轻微跳动或 safe area 间距不够精致。

## DV04: Long text and extreme data

目标: 验证真实社交产品中高风险文本不会破坏布局、字体、间距和信息层级。

样本数据:

- 1000 字中文帖子。
- 500 字中英混排帖子。
- 80 字地点。
- 40 字用户名。
- 40 字分类标签。
- 长 URL。
- 长 Telegram handle。
- 20 位金额或订单号。
- 6 位以上互动计数。
- 多段换行文本。

覆盖页面:

- `/`
- `/post/:id`
- `/create`
- `/profile`
- `/user/:id`
- `/chat`
- `/recharge`
- `/transactions`
- `/promote`
- `/promote/history`

通过标准:

- 正文可读，行高稳定。
- 元信息合理省略。
- 动作区不重叠。
- 卡片高度自然增长。
- 长 URL 不撑破容器。
- 金额和订单号可扫描。
- 中文和英文混排不造成异常间距。

失败分级:

- P0: 文本撑破页面或遮挡关键操作。
- P0: 金额、隐私、联系方式显示误导。
- P1: 多行文本破坏扫描节奏。
- P2: 省略点或 baseline 有轻微漂移。

## DV05: Image failure and slow loading

目标: 验证图片加载失败、慢网络、上传错误不会破坏内容流和媒体体验。

覆盖范围:

- Feed media。
- Detail media。
- Avatar。
- Profile cover。
- Upload tile。
- Upload progress。
- ImageLightbox。

执行状态:

- 图片 URL 404。
- 图片超慢加载。
- 上传中。
- 上传失败。
- 多图部分失败。
- Avatar 失败。

通过标准:

- 坏图有清楚但低噪声 fallback。
- Loading 不闪烁。
- 图片失败不破坏卡片比例。
- Lightbox 对失败图片有恢复或关闭路径。
- 上传失败提示不遮挡发布器。
- Avatar fallback 不抢正文。

失败分级:

- P0: 坏图导致页面布局塌陷。
- P0: 上传失败后无法删除或重试。
- P1: fallback 过重，抢内容注意力。
- P2: 占位文案或图标略显噪声。

## DV06: Color contrast and state expression

目标: 验证颜色不是只靠主观高级感，关键文本和状态必须可读。

必须测量:

- Muted text on page surface。
- Muted text on card surface。
- Brand button white text。
- Error text on error surface。
- White text on overlay。
- Disabled state。
- Link text。
- Badge text。
- Image overlay text。

覆盖页面:

- `/`
- `/create`
- `/post/:id`
- `/profile`
- `/promote`
- `/recharge`
- `/transactions`
- `/about`
- `/168wc`

通过标准:

- 正文级文本满足可读性。
- 辅助文字不能低到不可辨认。
- Error、success、warning 不只靠颜色表达。
- Disabled 状态既明显又不误导。
- 图片背景上文字有足够遮罩或背景处理。

失败分级:

- P0: 关键金额、错误、支付、发布 action 不可读。
- P1: 辅助说明在常见 surface 上对比偏低。
- P2: 非核心 badge 或 meta 对比略弱。

## DV07: Motion, loading and perceived stability

目标: 验证动效服务理解，不制造焦虑、眩晕或视觉噪声。

覆盖范围:

- 页面进入。
- Feed loading。
- Pull refresh。
- Sheet open/close。
- Payment confirm。
- ImageLightbox slide。
- Button pressed。
- Skeleton to content。

执行步骤:

1. 正常 motion 环境下观察。
2. 减少动态环境下观察。
3. 触发 loading 到内容切换。
4. 触发 sheet 和 lightbox 打开关闭。
5. 观察布局是否跳动。

通过标准:

- 动效短、明确、不抢内容。
- Loading 到内容切换不大幅跳动。
- Reduced motion 下没有强制大位移动效。
- Pull refresh 文案和状态同步。

失败分级:

- P0: 动效导致操作误触或任务中断。
- P1: Sheet、lightbox、loading 切换明显跳动。
- P2: 单个 micro-motion 稍显重。

## DV08: Responsive and safe-area stress

目标: 验证组件在窄屏、桌面、wide、safe area 下都不靠隐藏核心信息维持稳定。

视口:

- 360 x 740。
- 390 x 844。
- 1280 x 720。
- 1440 x 900。

覆盖重点:

- 发布工具栏。
- Feed action row。
- Chat input。
- Promote checkout bar。
- Recharge amount input。
- Profile header。
- About editorial layout。
- Admin table/mobile list。

通过标准:

- 窄屏只省略长文本，不挤掉图标。
- 底部 action 不遮挡内容。
- 桌面留白变成结构。
- Wide 下内容不变成孤岛。
- Safe area 与 sticky 元素关系稳定。

失败分级:

- P0: 窄屏重叠或关键按钮不可用。
- P1: 桌面出现手机壳居中或大空地。
- P2: 局部间距略松或略紧。

## Route to dynamic check matrix

`/`:

- 必测: DV01、DV04、DV05、DV06、DV07、DV08。
- 重点: Feed continuity、media fallback、action row、desktop structure。

`/create`:

- 必测: DV01、DV03、DV04、DV05、DV06、DV08。
- 重点: Composer、toolbar、image upload、location、privacy、contact。

`/post/:id`:

- 必测: DV01、DV02、DV04、DV05、DV06、DV07。
- 重点: Long post、media、lightbox、overlay。

`/profile`:

- 必测: DV01、DV02、DV03、DV04、DV06、DV08。
- 重点: Profile edit、tabs、identity hierarchy。

`/promote`:

- 必测: DV01、DV02、DV03、DV04、DV06、DV08。
- 重点: Picker、checkout、payment sheet、keyboard。

`/chat`:

- 必测: DV01、DV03、DV04、DV06、DV08。
- 重点: Message input、long message、desktop split。

`/recharge`:

- 必测: DV01、DV03、DV04、DV06、DV08。
- 重点: Amount input、QR、copy action。

`/transactions`:

- 必测: DV01、DV04、DV06、DV08。
- 重点: Record list scanability。

`/sponsor`:

- 必测: DV01、DV02、DV04、DV06、DV08。
- 重点: Balance、records、confirm states。

`/category/:categoryId`:

- 必测: DV01、DV04、DV05、DV06、DV08。
- 重点: Category list、empty、load more。

`/about`:

- 必测: DV01、DV06、DV07、DV08。
- 重点: Reading rhythm、desktop editorial layout。

`/404`:

- 必测: DV01、DV06、DV08。
- 重点: StatePlacement page。

`/168wc`:

- 必测: DV01、DV04、DV06、DV08。
- 重点: Admin table, mobile overflow, hardcoded utility debt。

## Release 1 dynamic gates

Release 1 完成后必须优先执行:

1. `/` initial error or empty: DV01、DV06、DV08。
2. `/create` auth state: DV01、DV04、DV06、DV08。
3. `/profile` auth state: DV01、DV04、DV06、DV08。
4. `/promote` auth state: DV01、DV04、DV06、DV08。

Release 1 通过标准:

- 状态 action 可键盘触达。
- Focus ring 不被裁切。
- 状态文案长文本不重叠。
- 移动端底部导航不遮挡状态 action。
- Muted copy 和 CTA 对比可读。
- 桌面状态不形成卡片孤岛。

如果 Release 1 只完成代码但没有这些动态证据，Phase 11 中只能标记 `partially proved`。

## Dynamic defect triage

P0:

- 键盘无法完成发布、支付、登录、返回、关闭弹层。
- Focus 不可见或跑到背景导致用户迷失。
- 移动键盘遮挡关键输入或提交。
- 关键金额、错误、发布、支付 CTA 不可读。
- 窄屏重叠导致内容不可操作。

P1:

- Tab 顺序明显跳跃但仍可完成任务。
- Sheet 背景可滚或关闭后焦点恢复不理想。
- 长文本影响扫描但不阻塞。
- Loading、motion、skeleton 到内容切换明显跳动。
- 桌面 composition 仍像手机壳。

P2:

- Focus ring 视觉不够精致但可见。
- 单个图标 baseline 或 hover 稍有漂移。
- 非核心 meta 对比略弱。
- 局部间距在极端数据下略紧。

## 本阶段结论

Phase 14 将动态体验从“以后要测”变成了可执行验证表。当前目标仍不能标记完成，因为这些动态检查尚未运行，也没有对应证据包。后续任何代码改造或截图补证，都必须回到本文件执行对应 DV 项，否则只能证明静态视觉，不能证明完整 UI/UE 质量。

# UI/UE 深度审计 Phase 13 - Release 1 代码改造执行规格

日期: 2026-06-26

目标: 将 Phase 12 的 Release 1 落地前审计继续推进为补丁级执行规格。此文件不是代码实现，而是下一步动代码时的边界、顺序、兼容策略和验收回填模板，确保后续实现符合精致感、呼吸感、简约感，并避免硬编码、临时方式和 CSS 强压。

## Release 1 范围

本 Release 只解决状态体系和登录墙体系:

1. `UIStateBlock` 和 `StateBlock` 支持 placement、density。
2. 状态样式从页面 class 迁移到系统合同。
3. 首页 feed 初始错误/空态和 footer 状态分离。
4. `EmptyStateCard` 移除 `py-*` utility spacing。
5. `AuthRequiredState` 支持 placement、density、context、preview、meta、secondaryAction。
6. create/profile/promote 三个高影响登录墙先迁移到 context-aware 状态。

明确不做:

1. 不做 Admin 设计债。
2. 不做 IconAction 合同。
3. 不做 Media/lightbox/upload 合同。
4. 不做 Promote payment sheet。
5. 不做 PageContentShell desktop split。
6. 不做全站 typography token 大改。

## 补丁批次 A: State API

### A1. `UIStateBlock.tsx`

目标:

- 让状态语义由 props 表达，而不是由页面 className 表达。

新增类型:

```ts
export type UIStatePlacement = 'page' | 'feed' | 'panel' | 'inline';
export type UIStateDensity = 'compact' | 'regular' | 'spacious';
```

新增 props:

```ts
placement?: UIStatePlacement;
density?: UIStateDensity;
titleLevel?: 'h2' | 'h3' | 'h4';
```

兼容策略:

- `inline={true}` 映射为 `placement="inline"`。
- `compact={true}` 映射为 `density="compact"`。
- 未传时默认 `placement="page"`、`density="regular"`。
- 保留旧 props，避免一次性破坏调用点。

DOM 合同:

```text
class="ui-state-block ui-state-block--{tone}"
data-state-placement="{placement}"
data-state-density="{density}"
```

禁止:

- 不新增页面专属状态类。
- 不用 className 判断 placement。

### A2. `LoadingState.tsx`

目标:

- 让 `StateBlock` 成为 `UIStateBlock` 新合同的主入口。

新增到 `StateBlockProps`:

```ts
placement?: UIStatePlacement;
density?: UIStateDensity;
titleLevel?: ComponentProps<typeof UIStateBlock>['titleLevel'];
```

兼容策略:

- `compact`、`inline` 继续可用。
- 新调用优先用 `placement`、`density`。

`LoadingBlock` 建议:

- 同步支持 `placement`、`density`。
- 暂时保留 `iconSize`，但新增语义 size 留到 Release 2。

### A3. `EmptyStateCard.tsx`

目标:

- 移除 `py-12`、`py-16`。

新增 props:

```ts
placement?: UIStatePlacement;
density?: UIStateDensity;
```

映射:

- 旧 `compact={true}` 映射 `density="compact"`。
- 默认 `placement="panel"` 或 `page` 需要按调用场景决定，但组件默认建议 `panel`，避免空态天然过重。

className:

```text
empty-state-card
```

禁止:

- 不再拼 Tailwind utility spacing。

## 补丁批次 B: State token 和 CSS

### B1. `foundation.css`

新增 token:

```css
--ui-state-page-min-height
--ui-state-feed-min-height
--ui-state-panel-min-height
--ui-state-inline-min-height
--ui-state-page-padding-block
--ui-state-feed-padding-block
--ui-state-panel-padding-block
--ui-state-inline-padding-block
--ui-state-density-compact-gap
--ui-state-density-regular-gap
--ui-state-density-spacious-gap
--ui-state-feed-icon-shadow
```

要求:

- token 可以复用已有 `--ui-space-*`、`--ui-line-*`、`--ui-shadow-*`。
- 不在 feature CSS 定义这些核心 token。

### B2. `02-core-controls.css`

新增选择器:

```css
.ui-state-block[data-state-placement='page']
.ui-state-block[data-state-placement='feed']
.ui-state-block[data-state-placement='panel']
.ui-state-block[data-state-placement='inline']
.ui-state-block[data-state-density='compact']
.ui-state-block[data-state-density='regular']
.ui-state-block[data-state-density='spacious']
```

迁移规则:

- `.ui-state-block--inline` 保留为兼容层，但内部应等价于 `data-state-placement='inline'`。
- `.ui-state-block--compact` 保留为兼容层，但内部应等价于 `data-state-density='compact'`。
- `.ui-feed-footer-state .ui-state-icon` 的具体 `8px 24px` shadow 改为 `--ui-state-feed-icon-shadow`。

视觉原则:

- `page`: 可以居中，但默认不使用卡片阴影。
- `feed`: 保留 feed 节奏，轻边界或无边界。
- `panel`: 用于局部容器内，min-height 比 page 小。
- `inline`: 用于字段和局部错误，不产生大块空白。

### B3. `ui-primitives-feedback.css`

调整:

- `.empty-state-card` 默认 shadow 降级，除非 placement 或 surface depth 另行指定。
- empty/info/error icon 背景保持 tone 差异，但降低视觉抢戏。

禁止:

- 不新增 `.home-*` 或 `.profile-*` 状态视觉补丁。

## 补丁批次 C: 首页 feed 状态迁移

### C1. `HomeDesktopFeedContent.tsx`

当前问题:

- `StateBlock` 使用 `surface-card home-desktop-error-state`。
- 首页 CSS 承担 feed state 的核心视觉。

改造目标:

```tsx
<StateBlock
  placement="feed"
  density="regular"
  tone="error"
  ...
/>
```

空态同理:

```tsx
<StateBlock
  placement="feed"
  density="regular"
  tone="empty"
  ...
/>
```

删除或降级:

- `surface-card` 不再作为初始错误/空态核心类。
- `home-desktop-error-state`、`home-desktop-empty-state` 只允许保留外层布局补充，不再定义 shadow、radius、background。

### C2. `DouyinFeed.tsx`

当前问题:

- 初始 empty/error 与 footer done 都用 `ui-feed-footer-state`。
- only-child CSS 承担初始状态布局。

改造目标:

初始状态:

```tsx
<StateBlock
  placement="feed"
  density="regular"
  ...
/>
```

footer 状态:

```text
ListLoadMoreState 继续用于 footer
className="ui-feed-footer-state"
```

建议拆分:

- 初始错误/空态使用 `ui-feed-state` 或仅依赖 `placement="feed"`。
- 已看完全部内容可以继续用 footer placement，但不要和初始空态混用。

### C3. `feed-scroll-shell.css`

当前问题:

```css
.feed-scroll-content > .ui-feed-footer-state:only-child
```

改造目标:

- 不再用 footer only-child 处理初始状态。
- 如果需要 only-child 布局，目标应是 feed placement:

```css
.feed-scroll-content > .ui-state-block[data-state-placement='feed']:only-child
```

但更优先让 `UIStateBlock` 自己通过 placement token 管理 min-height。

### C4. `home-feed-foundation.css`

当前问题:

- `.home-desktop-empty-state`、`.home-desktop-error-state` 定义状态核心视觉。

改造目标:

- 删除或弱化状态视觉，只保留必要的首页外部布局。
- 如果状态样式已经由 `UIStateBlock placement="feed"` 覆盖，最好移除这两个类。

## 补丁批次 D: 登录墙合同

### D1. `AuthRequiredState.tsx`

新增类型:

```ts
type AuthPlacement = 'page' | 'feed' | 'panel' | 'inline';
type AuthTone = 'card' | 'open' | 'hero' | 'quiet';
type AuthDensity = 'compact' | 'regular' | 'spacious';
type AuthContext = 'create' | 'profile' | 'promote' | 'recharge' | 'sponsor' | 'records' | 'generic';
```

新增 props:

```ts
placement?: AuthPlacement;
density?: AuthDensity;
context?: AuthContext;
preview?: ReactNode;
meta?: ReactNode;
secondaryAction?: ReactNode;
```

DOM 合同:

```text
data-auth-placement
data-auth-tone
data-auth-density
data-auth-context
```

兼容策略:

- 旧 `tone="card|open"` 保留。
- 未传 `context` 默认 `generic`。
- 未传 `placement` 默认 `page`。
- 未传 `density` 默认 `regular`。

结构建议:

```text
icon
title
description
preview
meta
actions
```

actions:

- primary action 使用现有 `ActionButton`。
- secondary action 作为 slot，不在组件内发明新按钮。

### D2. `ui-primitives-auth.css`

新增选择器:

```css
.ui-auth-required-card[data-auth-placement='page']
.ui-auth-required-card[data-auth-placement='panel']
.ui-auth-required-card[data-auth-placement='inline']
.ui-auth-required-card[data-auth-tone='hero']
.ui-auth-required-card[data-auth-tone='quiet']
.ui-auth-required-card[data-auth-density='compact']
.ui-auth-required-card[data-auth-context='create']
.ui-auth-required-card[data-auth-context='profile']
.ui-auth-required-card[data-auth-context='promote']
```

视觉原则:

- 默认 page 登录墙不使用过重 shadow。
- context 只控制轻微语义差异，不创建完全不同卡片。
- preview 区域应轻，不堆卡片。

### D3. 先迁移三个高影响调用点

`PostCreatePage`:

- context: `create`
- preview: 发布能力摘要，如图片、地点、隐私、联系方式。
- 目标: 未登录态体现发布任务，而不是通用登录要求。

`ProfileMobilePage`:

- context: `profile`
- preview: 资料、帖子、收藏、联系偏好。
- 目标: 体现身份资产。

`PromoteMobilePage`:

- context: `promote`
- preview: 选择帖子、设置周期、确认预算、获得曝光。
- 目标: 体现高价值流程。

延后迁移:

- `RechargeMobile`
- `SponsorMobilePage`
- `PromoteHistory`
- `App` fallback

原因:

- 先验证三个最影响首屏质感的页面，避免一次迁移太大。

## 补丁批次 E: 清理页面级强压

第一批清理:

- `.post-create-page .ui-auth-required-card`
- `.post-create-page .ui-auth-required-icon`
- `.post-create-page .ui-auth-required-wrap.ui-app-page-main`
- `.home-desktop-error-state`
- `.home-desktop-empty-state`
- `.feed-scroll-content > .ui-feed-footer-state:only-child`

清理原则:

- 如果样式代表语义，迁回组件 data attribute。
- 如果样式代表页面外部布局，保留但命名为 layout，不控制内部视觉。
- 不通过提高选择器权重解决问题。

## 兼容和风险控制

兼容:

- 所有旧 props 保留。
- `compact`、`inline` 不删除，只映射。
- `tone="card|open"` 不删除。
- className 不删除，但不得继续承载核心视觉。

主要风险:

- 状态 min-height 改动可能影响空态页面高度。
- 登录墙 shadow 降级可能让某些页面显得太轻。
- 首页移动状态从 footer class 迁出后，可能影响 pull refresh 或 scroll shell 的 only-child 行为。

风险应对:

- 每批只迁移少量调用点。
- 用 Phase 11 的证据包格式记录视觉回归。
- 先完成状态合同，再迁移页面。

## 必须回填的证据包

每完成一批补丁，追加记录:

```text
Scope:
Files changed:
Design dimension covered:
Screenshots:
Dynamic checks:
Hardcoding check:
Benchmark mapping:
Open risks:
Completion verdict:
```

Release 1 初次实现后，最低截图要求:

- `mobile-home` 初始错误或空态。
- `desktop-home` 初始错误或空态。
- `mobile-create` 未登录态。
- `mobile-profile` 未登录态。
- `mobile-promote` 未登录态。
- 对应桌面截图至少覆盖 create/profile/promote 中两个。

动态检查最低要求:

- Tab 能聚焦登录 CTA。
- 错误/空态 action 有可见 focus。
- 移动端底部导航不遮挡状态 action。
- 状态文案长文本不重叠。

## 完成判定

Release 1 只在以下条件满足时可标记为完成:

1. `UIStateBlock` 支持 placement 和 density。
2. `StateBlock`、`EmptyStateCard` 已接入新合同。
3. 首页桌面初始错误/空态不再靠 `surface-card home-desktop-error-state` 表达核心视觉。
4. 首页移动初始错误/空态不再复用 footer 状态语义。
5. `AuthRequiredState` 支持 context、preview、placement、density。
6. create/profile/promote 三个登录墙完成 context-aware 迁移。
7. 第一批页面级强压样式被删除或降级。
8. 没有新增裸视觉数字、utility spacing、`!important` 或页面强压选择器。
9. Phase 11 证据包至少回填为 `partially proved`。

如果只完成 API 但没有迁移首页和登录墙，判定为 `partially proved`。

如果只改首页样式但没有补合同，判定为 `not proved`。

## 本阶段结论

Phase 13 将 Release 1 从“可以开工”推进到“如何开工”。下一步如果进入代码实现，应严格按 A 到 E 批次推进: 先状态 API，再 token/CSS，再首页 feed 状态，再登录墙合同和三大调用点，最后清理页面强压。这样改动会让精致感、呼吸感、简约感变成设计系统能力，而不是单页补丁。

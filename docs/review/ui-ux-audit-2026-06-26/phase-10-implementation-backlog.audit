# UI/UE 深度审计 Phase 10 - 可实施优化 Backlog

日期: 2026-06-26

目标: 将 Phase 1 到 Phase 9 的审计结论收束为可进入设计系统和前端排期的实施 backlog。重点继续服务原始目标: 精致感、呼吸感、简约感，并严格遵守不硬编码、不临时方式、不 CSS 强压。

当前状态: 本文件是实施入口，不代表全站审计完成。它说明哪些问题已经有足够证据可以开工，哪些问题必须先补截图或动态验证。

## 总实施原则

1. 先改合同，再改页面。
2. 先改高复用组件，再改低复用页面。
3. 先减少视觉噪声，再增加装饰。
4. 先保留内容结构，再处理空态和错误态。
5. 任何新尺寸、颜色、阴影、动效都先进入 token 或合同层。
6. 页面只声明语义，不通过页面选择器强压组件内部像素。
7. 桌面不是移动端拉宽，桌面留白必须变成信息结构。

## 禁止清单

禁止在实施中引入以下做法:

1. 在 TSX 中新增裸视觉数字，例如 icon size、stroke、width、height、duration。
2. 在 feature CSS 中新增临时 `px`、`rem`、`rgba`、`box-shadow`，除非它们是新 token 的定义源。
3. 使用 `!important` 或 Tailwind `!` utility 修正组件尺寸。
4. 通过父级页面选择器修改子组件内部结构，例如 `.page .component > :not(...)`。
5. 为单个页面复制一套 login card、empty card、sheet、button。
6. 用 shadow、blur、gradient 解决本该由层级、间距、内容密度解决的问题。
7. 为了通过某个截图把内容隐藏、强制截断或改变语义。

## Release 1: 状态体系和首屏信任

目标: 先修最伤精致感的问题。首页失败态、登录墙、空态和加载态要像同一个产品，而不是不同页面各自拼出来。

### P0-01 FeedStateContract

证据来源:

- Phase 1: 首页失败态破坏首屏信任。
- Phase 2: 状态应该保留 feed 结构。
- Phase 5: 首页信息流方向正确，但失败态断掉内容流。
- Phase 8: 首页真实 feed 和错误恢复仍缺最终截图。

问题:

首页失败态当前会让内容流消失，只剩大空白和错误文案。对标 X 和 Threads，失败、空、加载应发生在内容流内部，而不是替代整个页面。

实施范围:

- 首页 feed 状态。
- 列表加载更多状态。
- Feed skeleton。
- Feed error retry。

合同建议:

```text
placement: feed | page | inline
density: compact | regular | spacious
tone: neutral | warning | error
actions: primary | secondary
preserveFrame: boolean
```

验收标准:

- 首页失败时仍看得到 feed 框架和恢复动作。
- 错误态不使用重卡片和大阴影。
- 移动端底部导航不遮挡恢复按钮。
- 桌面端错误态不变成孤岛。
- 不新增页面级强压 CSS。

### P0-02 AuthRequiredStateContract

证据来源:

- Phase 1: 发布、个人、推广未登录态高度同质。
- Phase 2: `AuthRequiredState` tone 不足。
- Phase 6: 登录墙模板过重且语义重复。
- Phase 8: 真实登录态仍缺，但未登录态问题已可开工。

问题:

发布、个人、推广都使用近似同一张登录卡，页面任务语义丢失。用户无法从未登录态理解该页面的价值。

实施范围:

- `AuthRequiredState`。
- 发布页未登录态。
- 个人页未登录态。
- 推广页未登录态。
- 充值、记录、赞助等需要登录的页面状态。

合同建议:

```text
placement: page | panel | inline | feed
tone: card | open | hero | quiet
density: compact | regular | spacious
context: create | profile | promote | wallet | records
slots: preview | meta | secondaryAction
```

验收标准:

- 发布页未登录态能看到发布能力摘要。
- 个人页未登录态能看到身份资产预期。
- 推广页未登录态能看到推广流程和价值。
- 卡片默认不使用强阴影。
- 页面不复制新的 login card。

### P0-03 StatePlacement 和 SurfaceDepth

证据来源:

- Phase 2: 过度依赖白卡片和阴影。
- Phase 4: ErrorBoundary、Skeleton、BottomSheet、Lightbox surface 不统一。
- Phase 6: Surface depth 是 P1 但会影响多个 P0 状态。

问题:

状态页、空态、错误态、弹层、卡片的材料层级没有统一语义。结果是普通页面状态也像浮层一样重。

实施范围:

- UI state block。
- Empty state。
- Error boundary fallback。
- Auth state。
- Surface section card。

合同建议:

```text
surfaceDepth: flat | hairline | raised | floating | modal | immersive
statePlacement: page | feed | panel | inline
```

验收标准:

- 页面普通状态默认使用 `flat` 或 `hairline`。
- `floating` 只用于 popover、sheet、active elevated panel。
- `modal` 只用于真正阻塞式弹层。
- about、auth、recharge 等页面的普通内容不再默认重阴影。

## Release 2: 图标动作和媒体系统

目标: 收敛最容易出现像素漂移的 action、icon、avatar、image、upload、lightbox。

### P0-04 IconActionContract

证据来源:

- Phase 4: Icon action 家族分裂。
- Phase 6: IconAction 是 P0。
- Phase 8: 组件高杠杆审计已完成，足够支持基础实施。

问题:

返回、发布、头像、关闭、Telegram、设置、select caret、sheet close、lightbox close 各自决定尺寸、stroke、背景、圆角和 hover。

实施范围:

- Icon button family。
- Topbar actions。
- Publish action。
- Profile/avatar action。
- Telegram contact action。
- Sheet close。
- Lightbox close。
- Setting row trailing action。

合同建议:

```text
size: xs | sm | md | lg
tone: quiet | muted | brand | danger | inverse
shape: control | pill | circle
context: topbar | sheet | lightbox | feed | profile | composer
```

验收标准:

- 同一 context 下 icon size 和 stroke 一致。
- 不再通过 className 字符串判断 back action。
- 不再出现 `!p-0` 这类强压。
- Telegram action 的 token 化方式作为样板复用。

### P0-05 MediaContract

证据来源:

- Phase 4: OptimizedImage、AvatarImage、ImageLightbox、Skeleton 存在媒体合同分散。
- Phase 5: PostMediaGrid、ImageUpload、ProfileHeaderCover 存在比例、fallback、placeholder、upload ring 债。
- Phase 6: Media 合同分散列为 P0。

问题:

图片尺寸、占位、坏图 fallback、头像 fallback、上传进度、lightbox 动效分散在多个组件中。能力强，但高级感不稳定。

实施范围:

- Feed media。
- Detail media。
- Avatar。
- Upload preview。
- Upload progress ring。
- Lightbox。
- Profile cover。

合同建议:

```text
mediaPreset: avatarThumb | profileAvatar | feedMedia | detailMedia | composerPreview | lightbox
fallbackTone: neutral | brand | anonymous | hidden
loadingTone: shimmer | subtle | none
motionPreset: fade | slide | immersive
```

验收标准:

- 内容流头像 fallback 默认低噪声，不抢正文。
- Feed 图片坏图有清楚但克制的 fallback。
- 上传进度环尺寸、stroke、duration 全部走 token。
- Lightbox close 和 motion 不再内联写 transition。
- Profile cover sizes 不再是散落字符串。

### P1-06 SkeletonPreset

证据来源:

- Phase 4: PageLoader 直接传 skeleton 数字。
- Phase 5: sponsor、recharge skeleton 有局部宽度。

问题:

加载态和真实内容不完全共享尺寸节奏，切换时可能跳动。

实施范围:

- Home feed skeleton。
- Detail skeleton。
- Profile skeleton。
- Recharge skeleton。
- Record list skeleton。

合同建议:

```text
preset: feed | detail | profile | record | payment
density: compact | regular | spacious
```

验收标准:

- 页面级 skeleton 不传裸 width/height。
- Skeleton 与真实内容的头像、标题、正文、媒体区域位置一致。
- Loading 不比真实内容更重。

## Release 3: 发布、筛选、字段和弹层

目标: 把发布编辑器、首页筛选、推广支付、资料编辑这些复杂控件收敛成同一套轻量输入语言。

### P0-07 CreatorFieldContract

证据来源:

- Phase 5: 发布字段和首页筛选字段重复。
- Phase 6: 发布/筛选字段控件重复列为 P0。

问题:

发布页分类字段、首页结构化筛选、设置行、picker、segmented、range field 都在表达相似控件，但视觉细节和交互结构不完全统一。

实施范围:

- 发布分类结构字段。
- 首页结构化筛选。
- 地点 picker。
- Boolean segmented。
- Number/range input。
- Picker button。

合同建议:

```text
FormFieldRow
PickerButton
SegmentedControl
RangeField
LocationPickerList
FieldOptionGrid
```

验收标准:

- 发布和筛选里的同类字段看起来属于同一产品。
- 必填错误不只靠颜色表达。
- 选中态不用 shadow 堆叠。
- 字段高度、间距、label、hint 都来自 token。

### P0-08 SheetContract

证据来源:

- Phase 4: BottomSheet 开放 className 过多，close/header/body 没有稳定合同。
- Phase 5: HomeStructuredFilterSheet、PromotePaymentSheet、ProfileDialog 视觉和可访问性不统一。
- Phase 8: Dialog focus trap 和 Escape 仍缺动态验证。

问题:

筛选 sheet、选择帖子 sheet、支付确认 sheet、资料编辑 dialog 的材料、动效、close、footer、键盘处理像不同系统。

实施范围:

- BottomSheet。
- Home structured filter sheet。
- Promote picker sheet。
- Promote payment sheet。
- Profile dialog。
- Image lightbox close control 只共享 action，不共享 sheet material。

合同建议:

```text
size: compact | regular | large | full
density: compact | regular | spacious
surfaceDepth: modal | floating
titleBehavior: fixed | scroll
footerBehavior: fixed | inline | none
keyboardBehavior: preserveInput | none
```

验收标准:

- Sheet header、body、footer 间距统一。
- Close action 使用 IconAction。
- Motion 使用全局 duration/easing token。
- Promote payment 不再以 utility animation 作为核心视觉。
- Escape、focus return、背景滚动锁需要后续动态验证。

### P0-09 ComposerToolbarContract

证据来源:

- Phase 5: 发布工具栏仍有历史遗留二次宽度合同。
- 实际修复历史: 图片摘要问题暴露了容器和按钮同时管理宽度的风险。

问题:

发布工具栏应该只有一个布局所有者。容器负责 5 等分，按钮只负责内容对齐和摘要省略。

实施范围:

- 发布页工具栏。
- ImageUpload toolbar trigger。
- Category、location、privacy、contact button。

合同建议:

```text
toolbarColumns: 5
itemMinWidth: 0
summaryBehavior: hidden | count | label | ellipsis
```

验收标准:

- 图片空态显示 `0/9`。
- 图片完成显示 `N/9`。
- 上传中显示 `上传中`。
- 隐私显示 `公开` 或 `匿名`。
- 联系方式显示 `显示` 或 `隐藏`。
- 地点只展示独立地点，不冒充分类结构化地点。
- 按钮自身不再携带五分之一宽度语义。

## Release 4: 桌面 composition 和页面呼吸感

目标: 解决桌面像移动壳居中的问题。呼吸感不是空地，而是有节奏的信息结构。

### P1-10 PageContentShellVariants

证据来源:

- Phase 2: `PageContentShell` variant 过粗。
- Phase 6: 桌面布局没有充分桌面化。

问题:

页面只有 narrow/fluid 不够。社交产品至少需要 feed、editor、readable、split、wide。

实施范围:

- PageContentShell。
- Home feed。
- Create editor。
- Chat。
- About。
- Profile。
- Promote。
- UserSpace。

合同建议:

```text
variant: feed | editor | readable | split | wide | fluid
gutter: compact | regular | spacious
rail: none | left | right | both
```

验收标准:

- Chat desktop 不再像窄手机壳。
- About desktop 从卡片岛变成 editorial layout。
- Profile/UserSpace 桌面留白变成身份和内容结构。
- 页面不通过单独 max-width 修补。

### P1-11 ChatDesktopSplit

证据来源:

- Phase 1: 聊天桌面健康度中低。
- Phase 2: 桌面聊天需要 split shell。

问题:

桌面聊天使用窄面板居中，空间浪费明显。

实施范围:

- Chat shell desktop。
- Conversation list 或上下文栏。
- Main conversation panel。

验收标准:

- 1280 x 720 下桌面聊天至少呈现双区结构或有明确上下文栏。
- 移动端继续保留单列沉浸式。
- 不用强行拉宽移动壳。

### P1-12 AboutEditorialLayout

证据来源:

- Phase 1: 关于页桌面像移动卡片放大。
- Phase 2: about 需要 editorial layout。

问题:

关于页大量白卡片和阴影切碎叙事，不够 Apple 式克制。

实施范围:

- About hero。
- Value sections。
- FAQ or story blocks。

验收标准:

- 桌面有主叙事区和辅助信息区。
- 默认 section 不使用重 shadow。
- 文案 line-height 和 max-width 服务阅读，不靠卡片撑层级。

## Release 5: 商业流程和后台

目标: 商业流程要可信、安静、可扫描；后台要单独治理，不拖累 C 端 P0。

### P1-13 CommerceFlowContract

证据来源:

- Phase 5: 推广、充值、赞助、记录容易变重。
- Phase 8: 充值、交易、赞助、推广完整流程缺截图。

问题:

余额、金额、二维码、记录、checkout、支付确认需要统一信任语言。当前局部 token 有基础，但流程之间仍像不同模块。

实施范围:

- Promote checkout。
- Promote payment confirm。
- Recharge amount。
- Recharge QR。
- Sponsor center。
- Transaction history。

合同建议:

```text
CommerceSummary
AmountInput
PaymentQrBlock
RecordList
CheckoutBar
PaymentConfirmSheet
```

验收标准:

- 金额、余额、费用、状态一眼可扫。
- QR 是主视觉，但说明不拥挤。
- 记录列表不靠重卡片区分每一项。
- Checkout 底栏不遮挡内容。

### P2-14 AdminDesignDebtContract

证据来源:

- Phase 3 和 Phase 6: Admin TSX utility/硬编码风险约 522 处。
- Phase 5: Admin 需要单独 Phase。

问题:

Admin 的硬编码债明显高于 C 端。它应该继承 foundation token，但可以有独立 density，不应该和 C 端 P0 混改。

实施范围:

- Admin dashboard。
- Admin table。
- Admin badge。
- Admin action button。
- Admin metric card。
- Admin mobile list。

合同建议:

```text
AdminBadge
AdminTable
AdminActionButton
AdminMetricCard
AdminMobileList
AdminDensity
```

验收标准:

- Admin TSX 不再主导视觉像素。
- Admin 子主题继承 foundation token。
- 移动 admin 不横向溢出。
- C 端 P0 不被 admin 大改阻塞。

## 视觉验收批次

每完成一个 Release，至少需要截图:

1. mobile 390 x 844。
2. desktop 1280 x 720。
3. 如果改动影响桌面 composition，补 wide 1440 x 900。
4. 如果改动影响窄屏工具栏、支付、键盘，补 compact mobile 360 x 740。

验收截图文件继续进入:

`docs/review/ui-ux-audit-2026-06-26/screenshots/`

命名继续遵循 Phase 9:

`{batch}-{order}-{viewport}-{route-or-flow}-{state}.png`

## 动态验收批次

每个 Release 需要绑定以下动态检查:

1. Keyboard tab order。
2. Focus visible 是否被裁切。
3. Dialog Escape 和焦点恢复。
4. Mobile keyboard 输入可见性。
5. Long text、long location、long username、long Telegram。
6. Image fail 和 slow loading。
7. Muted text、error、disabled、brand button contrast。

说明: 截图可以证明视觉状态，但不能证明可达性完成。动态检查必须独立记录。

## 排期建议

第一优先:

1. P0-01 FeedStateContract。
2. P0-02 AuthRequiredStateContract。
3. P0-03 StatePlacement 和 SurfaceDepth。

原因: 直接修复首屏信任、通用登录墙和重卡片，是截图证据最充分的问题。

第二优先:

1. P0-04 IconActionContract。
2. P0-05 MediaContract。
3. P0-09 ComposerToolbarContract。

原因: 这些会减少全站像素漂移，并防止发布页摘要类问题复发。

第三优先:

1. P0-07 CreatorFieldContract。
2. P0-08 SheetContract。

原因: 发布、筛选、支付、编辑弹层会因此变成同一套控件语言。

第四优先:

1. P1-10 PageContentShellVariants。
2. P1-11 ChatDesktopSplit。
3. P1-12 AboutEditorialLayout。

原因: 主要提升桌面呼吸感，但需要更多截图回归。

第五优先:

1. P1-13 CommerceFlowContract。
2. P2-14 AdminDesignDebtContract。

原因: 商业流程需要真实流程截图；Admin 债大但不应抢 C 端 P0。

## 完成判定

本 backlog 完成不等于原始目标完成。原始目标完成必须同时满足:

1. Phase 9 的 Batch A 到 E 至少核心状态有截图证据。
2. P0 合同实施后有移动和桌面回归截图。
3. 动态验证清单至少覆盖键盘、focus、dialog、移动键盘、长文本、坏图、对比度。
4. 硬编码、临时方式、CSS 强压风险有新的静态证据证明下降。
5. 关键页面能对齐 X、Threads、Instagram、Apple 的内容优先、低噪声、克制材料和清晰动线原则。

## 本阶段结论

Phase 10 把审计结论转换成了可排期、可验收、可防止临时修补的实施 backlog。当前目标仍保持进行中，因为尚未完成真实登录态截图、动态验证和 P0 合同落地。但从现在开始，后续实现不应该再随机修页面，而应按 Release 1 到 Release 5 逐步收敛设计系统。

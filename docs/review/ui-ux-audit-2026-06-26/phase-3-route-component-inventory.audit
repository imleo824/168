# UI/UE 深度审计 Phase 3 - 路由与组件覆盖矩阵

日期: 2026-06-26

目标: 将“所有页面、所有组件、所有布局、所有字体、所有颜色、所有间距”的审计范围拆成可执行矩阵，明确哪些页面已有截图证据，哪些页面需要补登录态或真实数据，哪些核心组件应优先升级，避免后续通过硬编码或局部 CSS 强压解决问题。

## 当前路由清单

来自 `src/App.tsx` 的主路由:

1. `/`
页面: Home。
当前证据: 已有桌面和移动截图，但本地数据失败，只能审计顶部、频道、筛选、失败态、外壳。
优先级: P0。
核心问题: 首页错误态替代了信息流，首屏信任损失最大。
下一步: 统一 feed 内联错误态和 skeleton 框架。

2. `/create`
页面: PostCreate。
当前证据: 已有未登录截图。已登录编辑器、图片、地点、隐私、联系方式、分类字段仍需补截图。
优先级: P0。
核心问题: 未登录态过通用，发布任务语义不足。
下一步: 发布页使用更轻的 task-aware auth state。

3. `/chat`
页面: Chat。
当前证据: 已有桌面和移动截图。
优先级: P1。
核心问题: 移动端较成熟，桌面端仍像手机壳居中。
下一步: 桌面 split shell，不再把 mobile shell 拉到桌面。

4. `/recharge`
页面: RechargeMobile。
当前证据: 缺截图。
优先级: P1。
核心问题: 使用 `SurfaceSectionCard` 较多，容易出现充值页卡片堆叠、层级过重。
下一步: 补登录态和已登录支付流程截图，审计金额选择、二维码、支付卡片、底部动作。

5. `/transactions`
页面: TransactionHistoryMobile，外层 `RequireAuthRoute`。
当前证据: 缺截图。
优先级: P1。
核心问题: 记录列表大量使用 record card，空态使用 `EmptyStateCard`，需要确认列表密度和空态轻重。
下一步: 补有记录、无记录、加载失败三态。

6. `/profile`
页面: ProfileMobile。
当前证据: 已有未登录截图，缺真实资料和帖子列表截图。
优先级: P0。
核心问题: 未登录态与发布、推广重复，个人身份资产表达不足。
下一步: profile pre-auth state 增加资料/帖子/收藏语义。

7. `/sponsor`
页面: SponsorMobile。
当前证据: 缺独立截图。
优先级: P1。
核心问题: 赞助页承载商业价值，但当前同样依赖通用登录状态和空态卡片。
下一步: 补登录态、可赞助列表、空列表、操作确认状态。

8. `/post/:id`
页面: PostDetail。
当前证据: 缺真实帖子截图。
优先级: P0。
核心问题: 帖子详情是内容平台核心页面，尚未有像素证据。
下一步: 补图文帖、纯文本帖、多图帖、长文本帖、错误/删除态。

9. `/user/:id`
页面: UserSpace。
当前证据: 缺真实用户空间截图。
优先级: P1。
核心问题: 使用 `PageContentShell variant="fluid"` 和 `ui-page-card-shell`，桌面可能继续出现卡片壳过重。
下一步: 补移动和桌面用户主页，审计头像、资料、列表、空态。

10. `/promote`
页面: PromoteMobile。
当前证据: 已有未登录截图，缺真实推广流程。
优先级: P0。
核心问题: 高价值页面未登录态缺价值解释，已登录流程需要审计步骤动线和预算信息密度。
下一步: 补选择帖子、配置推广、确认支付、成功态。

11. `/promote/history`
页面: PromoteHistory。
当前证据: 缺截图。
优先级: P1。
核心问题: 混合使用 `SurfaceSectionCard`、`EmptyStateCard` 和记录卡，可能卡片密度高。
下一步: 补有历史、无历史、编辑摘要、错误态。

12. `/404`
页面: NotFound。
当前证据: 缺截图。
优先级: P2。
核心问题: 当前 NotFound 存在 `min-h-[45vh]`、`gap-4`、`gap-3` 这类局部布局类，和“不硬编码”目标有冲突。
下一步: 使用统一状态组件和 token 化 page state。

13. `/168wc`
页面: Admin。
当前证据: 缺截图。
优先级: P2 或单独 Phase。
核心问题: admin 存在大量 Tailwind hardcoded utility、颜色和尺寸，和 C 端 token 体系差距明显。
下一步: 明确 admin 是否纳入本轮用户端精致化范围。若纳入，需要独立 admin design system pass。

14. `/about`
页面: BrandAbout。
当前证据: 已有桌面和移动截图。
优先级: P1。
核心问题: 样式 token 化较好，但 TSX 内仍有 `h-11`、`px-5`、`mt-4`、`gap-3` 等布局硬编码类；桌面 composition 像移动卡片放大。
下一步: 将 about page 的 action、section、list spacing 收敛到 brand-about tokens。

15. `/category/:categoryId`
页面: CategoryFeedMobile。
当前证据: 缺分类页截图。
优先级: P1。
核心问题: 分类页是内容发现核心入口，当前只知道存在 `LOAD_MORE_ROOT_MARGIN = '1000px'` 这类行为常量，缺视觉证据。
下一步: 补分类信息流、空分类、加载更多、筛选切换。

## Overlay 路由覆盖

当前支持 overlay 的路由:

- `/post/:id`
- `/user/:id`
- `/category/:categoryId`
- `/about`

风险:

- overlay 页和普通页共享页面组件，但进入方式不同。像素审计不能只看直接访问，还要看从首页进入 overlay 的层级、返回、滚动锁、背景保留。
- 当前 Phase 1 只覆盖了直接页面截图，overlay 场景仍缺证据。

建议:

- 对 `/post/:id`、`/user/:id`、`/category/:categoryId` 至少补一次从首页点击进入的 overlay 截图。
- 检查 overlay topbar、背景层、滚动恢复、底部导航是否出现视觉冲突。

## 当前顶层 UI 组件清单

顶层 UI 组件共 30 个:

1. `ActionButton`
状态: 已深读。
风险: variant 只有 `primary|brand|muted|success|disabled`，缺少 `danger`、`ghost`、`tonal`、`link` 等语义，页面容易通过 className 补。
建议: 扩展语义 variant，并将高度、padding、字体完全交给 button token。

2. `AppPage`
状态: 已深读。
风险: 只负责页面根壳，缺少 route role、layout density、surface intent。
建议: 支持 `surface="page|card|transparent"` 和 `motion="enter|none"` 之类语义，避免每页 className 堆叠。

3. `AuthRequiredState`
状态: 已深读于 Phase 2。
风险: `tone` 只有 `card|open`，导致多页面登录墙高度同质。
建议: 增加 `inline|panel|hero` 与页面任务 slot，这是 P0。

4. `AvatarImage`
状态: 待深读。
风险: 头像是社交产品核心视觉资产，需要审计 fallback、尺寸、边框、加载失败。
建议: 后续单独做 identity component pass。

5. `BottomSheet`
状态: 待深读。
风险: 浮层最容易出现阴影、圆角、safe area 和滚动冲突。
建议: 和 dialog、lightbox 统一 surface depth。

6. `EmptyStateCard`
状态: 已深读。
风险: 默认拼接 `py-12|py-16`，属于组件内 Tailwind 间距硬编码；视觉上也默认 card shadow。
建议: 改为 `density` prop 映射 token 类，不再拼接 `py-*`。

7. `ErrorBoundary`
状态: 待深读。
风险: 错误边界是信任恢复入口，必须和首页错误态统一。
建议: 纳入统一 state block 合同。

8. `HeaderSelectAction`
状态: 待深读。
风险: topbar 中的选择控件需要和 PageHeader 尺寸合同一致。
建议: 与 topbar action token 绑定。

9. `IconButton`
状态: 待深读。
风险: icon-only 操作需要严格检查命中区、aria-label、pressed state。
建议: 与 TopbarIconButton 合并或建立共享 icon action 合同。

10. `ImageLightbox`
状态: 待深读。
风险: 图片沉浸浏览决定媒体质感，需要审计背景、关闭按钮、分页、手势。
建议: 后续媒体组件 pass 覆盖。

11. `LinkifiedText`
状态: 待深读。
风险: 文本链接样式会影响内容流高级感和可读性。
建议: 与正文 typography token 绑定。

12. `ListLoadMoreState`
状态: 待深读。
风险: 信息流加载更多状态会影响 X/Threads 式连续阅读体验。
建议: 与 feed state 合同统一。

13. `LoadingState`
状态: 已深读。
风险: `InlineSpinner` 接收 number size，容易诱导局部传硬编码数字。
建议: size 改为 `xs|sm|md|lg` 映射 token，保留 escape hatch 但不默认使用。

14. `OptimizedImage`
状态: 静态扫描命中。
风险: `sizes` 内有 `96px`、`180px`、`860px`、`420px` 等硬编码。图像性能合理，但和设计 token 分离。
建议: 将图片尺寸策略提取为 media tokens 或常量表。

15. `PageContentShell`
状态: 已深读。
风险: variant 只有 `narrow|fluid`，无法表达 feed、editor、split、wide。
建议: 扩展为 `readable|feed|editor|split|wide|fluid`，这是 P0/P1。

16. `PageHeader`
状态: 已深读。
风险: variant 有 `standard|home|detail|transparent`，结构较好；但页面仍会通过 className、titleClassName、slot className 定制，存在绕开合同的空间。
建议: 增加更明确的 title density、action density、scroll behavior 合同。

17. `PageLoader`
状态: 使用点已扫描，待深读。
风险: 加载页必须避免和 skeleton 重复。
建议: 统一 loading block、skeleton、route loader 的使用边界。

18. `ProfileIconButton`
状态: 待深读。
风险: 顶部身份入口是高频元素，需要审计头像、badge、匿名态。
建议: identity action pass 覆盖。

19. `PublishIconButton`
状态: 待深读。
风险: 发布入口是核心转化点，需要确认桌面/移动、topbar/bottom nav 视觉一致。
建议: 与 ActionButton 和 topbar action 合同对齐。

20. `SegmentTabs`
状态: 已深读。
风险: TSX 内拼接 `grid`、`gap-0.5`、`gap-0`、`truncate` 等 utility，并且通过 className 是否包含 `profile-tabbar` 决定 label 展示，属于隐性契约。
建议: 用 `density`、`showLabelMode`、`layout` prop 替代 className 字符串推断。

21. `SettingRow`
状态: 待深读。
风险: 设置项通常影响间距和信息密度。
建议: 纳入 form/settings component pass。

22. `Skeleton`
状态: 使用点已扫描，待深读。
风险: skeleton 需要和真实卡片尺寸一致，否则加载到内容会跳动。
建议: 与 feed card、detail shell、profile shell 同步。

23. `SurfaceSectionCard`
状态: 已深读。
风险: `tone` 只有 `glass|solid|muted`，缺少 depth 语义；padding 可用 `paddingClassName` 任意注入，容易绕开 token。
建议: 改为 `depth="flat|hairline|raised|floating"` 和 `density="compact|regular|spacious"`。

24. `TelegramContactIconButton`
状态: 待深读。
风险: 联系方式入口牵涉隐私和信任，视觉应克制。
建议: 与 contact/privacy 状态统一。

25. `TopbarActions`
状态: 待深读。
风险: 多 action 排列可能影响窄屏。
建议: 与 PageHeader action slot 合同绑定。

26. `TopbarBackIcon`
状态: 待深读。
风险: 图标本身小，但返回认知重要。
建议: 确认 stroke、尺寸、RTL/语言无关性。

27. `TopbarIconButton`
状态: 待深读。
风险: 与 IconButton、PublishIconButton、ProfileIconButton 可能有重复合同。
建议: 合并 icon action 语义。

28. `UIStateBlock`
状态: 已深读。
风险: 有 tone、compact、inline，但缺少 placement 和 density；title 固定 h3，无法表达 page/feed/panel。
建议: 增加 `placement="page|feed|panel|inline"`，title level 可配置。

29. `UIText`
状态: 已深读。
风险: variant 只有 `caption|meta|body|title`，不足以表达 page title、section title、body readable、label。
建议: 扩展 typography variant，但仍映射 token 类。

30. `uiTypes`
状态: 已深读。
风险: `SurfaceTone = glass|solid|muted` 过粗，无法表达质感深度。
建议: 增加 surface depth 和 density 类型，作为组件 API 基础。

## 静态扫描发现的硬编码风险

说明: token 文件内的基础色值、spacing、radius、shadow 是允许的设计系统源头；风险主要在页面 TSX、feature TSX、组件 TSX 和非 token CSS 中出现的局部尺寸/间距/颜色。

1. `BrandAbout.tsx`
命中: `h-11`、`px-5`、`mt-4`、`gap-3`、`gap-2`。
风险: 关于页虽然 CSS token 化较好，但结构层还在写 Tailwind 间距，后续很难统一桌面 editorial rhythm。
建议: 替换为 `brand-about-action`、`brand-about-grid`、`brand-about-keyword-list` 等语义类，间距放 token。

2. `NotFound.tsx`
命中: `min-h-[45vh]`、`gap-4`、`gap-3`。
风险: 404 是典型 page state，却没有复用统一 state layout。
建议: 用 `UIStateBlock placement="page"` 或 AuthRequiredState 的轻量 page variant。

3. `CategoryFeedMobile.tsx`
命中: `LOAD_MORE_ROOT_MARGIN = '1000px'`。
风险: 行为常量可以存在，但应命名为 feed prefetch token/constant，避免散落在页面。
建议: 提取为 `FEED_LOAD_MORE_ROOT_MARGIN` 或 design/performance contract。

4. `ImageUpload.tsx`
命中: `grid gap-2.5`、`line-clamp-2 text-center`。
风险: 图片上传是发布核心组件，间距和错误态应由 upload primitive CSS 控制。
建议: 上传组件暴露 density 或 placement，错误提示用 `.image-upload-error-text`。

5. `OptimizedImage.tsx`
命中: responsive sizes 内具体 px。
风险: 这不是视觉硬编码 CSS，但设计和图片性能策略脱节。
建议: 提取 media size presets，如 `thumb|feed|detail` 对应常量。

6. `AdminPage.tsx`
命中: 大量 `text-[10px]`、`px-*`、`py-*`、`bg-gray-*`、`text-gray-*`、`rounded-*`。
风险: 如果 admin 纳入目标，它是目前最大硬编码债。
建议: 不要混在 C 端优化里修。单独建立 admin token 和 table/form/card primitive。

## 样式系统问题定位

1. 布局层
当前问题: `PageContentShell` variant 过少，页面不得不通过 className 和 feature CSS 继续补 layout。
后果: 桌面端出现手机壳居中、卡片岛、空白不成结构。
修法: 先扩展 shell variant，再替换页面调用。

2. 状态层
当前问题: `UIStateBlock` 已有基础，但 `EmptyStateCard`、`AuthRequiredState`、首页错误态没有统一 placement。
后果: 错误、空、登录看起来不像同一套产品语言。
修法: 统一 state placement 和 density。

3. Surface 层
当前问题: `SurfaceSectionCard` tone 表达颜色/材质，不表达层级深度。
后果: 页面默认进入 card+shadow，而不是 flat/hairline。
修法: 新增 depth token，并将默认 depth 调低。

4. 字体层
当前问题: `UIText` variant 过少，页面标题、section 标题、卡片标题、正文说明容易混用。
后果: 视觉层级偏硬，不够克制。
修法: 扩展 typography variant，降低说明文字 weight。

5. 控件层
当前问题: `ActionButton`、`IconButton`、`TopbarIconButton`、`PublishIconButton` 语义分散。
后果: 不同入口的 hover、active、size、圆角可能不一致。
修法: 建立 action primitive contract，按钮只声明 intent、density、shape。

## 建议的 Phase 4 实施顺序

1. P0: 重构 state 组件合同
范围: `UIStateBlock`、`EmptyStateCard`、`AuthRequiredState`、首页失败态。
目标: 所有 page/feed/panel/inline 状态统一，不再出现大白卡复制感。

2. P0: 扩展 page shell variants
范围: `PageContentShell`、`AppPage`。
目标: 支持 feed、editor、readable、split、wide，先解决桌面呼吸感不是“空地”的问题。

3. P1: Surface depth token 化
范围: `SurfaceSectionCard`、empty/auth/card CSS、brand about、recharge/promote/history cards。
目标: 默认 flat/hairline，shadow 只给浮层。

4. P1: 清理 C 端硬编码 utility
范围: `BrandAbout.tsx`、`NotFound.tsx`、`ImageUpload.tsx`、`OptimizedImage.tsx`、`CategoryFeedMobile.tsx`。
目标: 页面结构只声明语义，间距尺寸回到 CSS/token。

5. P2: Admin 单独设计债
范围: `AdminPage.tsx`。
目标: 如果纳入全站目标，单独建立 admin UI primitives；不建议和用户端 P0 混改。

## 本阶段未完成证据

- 缺登录态和真实数据截图。
- 缺 `/post/:id`、`/user/:id`、`/category/:categoryId` 的 overlay 场景截图。
- 缺充值、交易记录、赞助、推广历史、404 的截图。
- 部分 UI 组件只完成清单审计，尚未逐行深读。
- 尚未做浏览器可达性检查、键盘焦点检查、对比度测量和真实响应式回归。

结论: 目标尚未完成，但现在已经有了完整推进地图。下一轮最值得做的是 Phase 4 P0: 统一状态组件合同和页面壳 variants，这是最符合“精致感、呼吸感、简约感”且最不容易变成临时 CSS 补丁的路径。

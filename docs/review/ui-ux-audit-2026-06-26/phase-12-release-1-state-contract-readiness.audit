# UI/UE 深度审计 Phase 12 - Release 1 状态体系落地前审计

日期: 2026-06-26

目标: 基于当前代码状态，审计 Phase 10 中 Release 1 的可落地性。Release 1 聚焦 `FeedStateContract`、`AuthRequiredStateContract`、`StatePlacement` 和 `SurfaceDepth`。本阶段不改业务代码，只确认哪些文件已经具备基础、哪些位置会诱发硬编码、后续最小安全实施顺序是什么。

## 本阶段读取证据

状态组件:

- `src/ui/UIStateBlock.tsx`
- `src/ui/AuthRequiredState.tsx`
- `src/ui/EmptyStateCard.tsx`
- `src/ui/LoadingState.tsx`
- `src/ui/ListLoadMoreState.tsx`
- `src/ui/ErrorBoundary.tsx`
- `src/ui/Skeleton.tsx`

首页状态入口:

- `src/features/home/HomeFeedContent.tsx`
- `src/features/home/HomeMobileFeedContent.tsx`
- `src/features/home/HomeDesktopFeedContent.tsx`
- `src/features/home/HomeDouyinFeed.tsx`
- `src/features/feed/DouyinFeed.tsx`
- `src/features/home/HomeFeedSkeletons.tsx`

样式和 token:

- `src/styles/tokens/foundation.css`
- `src/styles/02-core-controls.css`
- `src/styles/system/ui-primitives-feedback.css`
- `src/styles/system/ui-primitives-auth.css`
- `src/styles/system/feed-scroll-shell.css`
- `src/styles/features/home-feed-foundation.css`
- `src/styles/features/create-promote-foundation.css`
- `src/styles/features/create-promote-submit.css`

调用点搜索:

- `AuthRequiredState`
- `StateBlock`
- `UIStateBlock`
- `EmptyStateCard`
- `ListLoadMoreState`
- `LoadingBlock`
- `InlineSpinner`

## 总体判断

Release 1 可以开工，但不能从页面 CSS 开始。

当前已有很好的基础:

- `UIStateBlock` 已集中承载 title、description、icon、action、secondaryAction、tone、aria。
- `StateBlock` 已经作为 `UIStateBlock` 的便利封装。
- `AuthRequiredState` 已经集中承载登录墙，调用范围清楚。
- `ListLoadMoreState` 已经集中承载加载更多。
- 首页桌面和移动都已经走共享状态组件，不是完全散落。
- foundation 里已经有状态相关 token，如 `--ui-state-block-min-height`、`--ui-state-icon-size`、`--ui-list-loadmore-min-height`。

当前最大问题:

- 状态组件只有 `compact` 和 `inline`，没有 `placement` 与 `density`。
- 登录墙只有 `tone="card|open"`，没有 context、preview、placement。
- 首页桌面错误态仍用 `surface-card home-desktop-error-state` 组合做局部视觉。
- 首页移动错误/空态复用 `ui-feed-footer-state`，语义上像 footer，不像 feed placement。
- `EmptyStateCard` 仍拼接 `py-12`、`py-16` 这类 utility spacing。
- `InlineSpinner` 和多个调用点仍传裸 `size={14|16|18}`。
- 发布页对 `.ui-auth-required-card` 做页面级覆盖，说明组件 API 不足。
- `ui-feed-footer-state .ui-state-icon` 仍存在具体 `8px 24px` shadow，和“低噪声、轻状态”目标冲突。

## P0 发现

### P0-01 `UIStateBlock` 缺少 placement 合同

当前状态:

`UIStateBlock` props 包含:

```text
tone
compact
inline
role
ariaLive
className
```

问题:

`compact` 和 `inline` 是形态开关，不足以表达页面状态语义。首页 feed 错误、404 页面状态、sheet 内状态、列表加载更多，都需要不同 placement。现在调用方只能通过 `className` 追加 `home-desktop-error-state`、`ui-feed-footer-state`、`user-space-state-block` 等局部类完成布局。

影响:

- 页面继续决定状态块像素。
- 同类错误/空态在不同页面视觉重量不同。
- 后续如果直接修首页，很容易写新的 `.home-feed-error` 强压。

建议:

新增:

```text
placement: page | feed | panel | inline
density: compact | regular | spacious
```

兼容策略:

- 保留 `compact` 和 `inline`，但内部映射到新 `density` 和 `placement`。
- 新代码只用 `placement` 与 `density`。
- `className` 只允许作为布局补充，不承载核心状态视觉。

### P0-02 `AuthRequiredState` 缺少页面任务语义

当前状态:

`AuthRequiredState` props 只有:

```text
title
description
actionLabel
onAction
icon
actionIcon
actionVariant
tone: card | open
className
```

调用范围:

- `PostCreatePage`
- `ProfileMobilePage`
- `PromoteMobilePage`
- `RechargeMobile`
- `SponsorMobilePage`
- `PromoteHistory`
- `App` 级权限保护 fallback

问题:

所有页面只能传标题和描述，导致发布、资料、推广、充值、赞助等页面语义都挤在同一种卡片里。发布页已经出现 `.post-create-page .ui-auth-required-card` 和 `.post-create-page .ui-auth-required-icon` 页面级覆盖，这是 API 不够造成的信号。

影响:

- 登录墙继续像同一张卡复制。
- 页面越多，覆盖越多。
- 很难表达 X/Threads/Instagram 式“当前任务价值预览”。

建议:

新增:

```text
placement: page | feed | panel | inline
tone: card | open | hero | quiet
context: create | profile | promote | recharge | sponsor | records | generic
density: compact | regular | spacious
preview
meta
secondaryAction
```

优先迁移:

1. `PostCreatePage`: create context，展示发布能力轻摘要。
2. `ProfileMobilePage`: profile context，展示身份资产轻摘要。
3. `PromoteMobilePage`: promote context，展示流程和价值轻摘要。
4. `RechargeMobile`、`SponsorMobilePage`、`PromoteHistory`: commerce contexts。
5. `App` fallback: generic context。

### P0-03 首页桌面状态仍是局部 surface card

当前状态:

`HomeDesktopFeedContent` 中:

- 错误态使用 `StateBlock`。
- 空态使用 `StateBlock`。
- 但 className 仍传 `surface-card home-desktop-error-state`、`surface-card home-desktop-empty-state`。

样式中:

- `.home-desktop-empty-state`
- `.home-desktop-error-state`
- `box-shadow: var(--ui-home-feed-state-shadow)`
- `border-radius: var(--ui-ins-card-radius)`
- `min-height: calc(var(--ui-space-8) * 4)`

问题:

桌面 feed 状态应该是 `placement="feed"`，而不是首页自己决定 border、radius、shadow、min-height。现在的写法虽然 token 化，但语义仍在页面 CSS。

影响:

- 首页错误态会继续偏孤岛。
- Category feed、UserSpace feed 也可能复制类似类。
- 后续无法证明状态体系统一。

建议:

先让 `StateBlock` 支持 `placement="feed"`，再把首页桌面改为:

```text
<StateBlock placement="feed" density="regular" tone="error" />
```

随后删除或降级 `home-desktop-error-state` 的核心视觉职责。

### P0-04 首页移动状态复用 footer 语义

当前状态:

`DouyinFeed` 中:

- 空态 `EmptyState` 使用 `StateBlock className="ui-feed-footer-state"`。
- 错误态 `ErrorState` 使用 `StateBlock className="ui-feed-footer-state"`。
- 加载更多、已看完也使用 `ui-feed-footer-state`。
- `feed-scroll-shell.css` 对 `.feed-scroll-content > .ui-feed-footer-state:only-child` 做 only-child 布局。

问题:

初始空态/错误态和 footer done state 是不同语义。现在它们共享 `ui-feed-footer-state`，并通过 only-child 选择器改变高度，属于“状态语义不足导致 CSS 选择器补逻辑”。

影响:

- 移动端初始错误态容易被当成 footer 状态处理。
- 布局由 DOM 位置和 only-child 决定，不够稳定。
- 未来加入 feed 内联错误、推荐控件或 skeleton 后容易破。

建议:

新增:

```text
placement="feed"
stateRole="initial" | "footer" 可选
```

或者拆为:

- `ui-feed-state`
- `ui-feed-footer-state`

但更推荐由 `UIStateBlock placement="feed"` 承担初始状态，由 `ListLoadMoreState placement="feed-footer"` 承担 footer。

### P0-05 `EmptyStateCard` 仍有 utility spacing

当前状态:

`EmptyStateCard` 拼接:

```text
compact ? 'py-12' : 'py-16'
```

问题:

这是明确的非合同间距。虽然组件集中，但 spacing 仍来自 Tailwind utility，不符合“不硬编码、不临时、不 CSS 强压”。

影响:

- 所有使用 `EmptyStateCard` 的页面会继承固定纵向重量。
- 状态 density 无法细分 page/feed/panel。

建议:

`EmptyStateCard` 改为:

```text
placement
density
```

类名只保留:

```text
empty-state-card
```

具体 padding 由 `.empty-state-card[data-state-density]` 或 `UIStateBlock` placement 控制。

## P1 发现

### P1-01 `InlineSpinner` size 仍是裸 number

当前状态:

`InlineSpinner` 接收 `size?: number`，默认 `18`。调用点出现:

- `size={14}`
- `size={16}`
- `size={18}`
- `size={12}`
- `size={13}`

问题:

spinner 尺寸是 control/action 状态的一部分，应该走 `xs|sm|md|lg` 或 context token。

建议:

新增:

```text
size: xs | sm | md | lg
```

保留 number escape hatch 但标注不推荐，新代码只用语义 size。

### P1-02 `LoadingBlock` 有 `46svh` 裸视口高度

当前状态:

`.ui-loading-block` 使用:

```text
min-height: 46svh
```

问题:

这是状态 placement 的典型职责，不应在 loading block 默认值里写固定视口比例。

建议:

迁移为:

```text
--ui-state-page-min-height
--ui-state-feed-min-height
--ui-state-panel-min-height
```

`LoadingBlock placement="page"` 才使用 page min-height。

### P1-03 Feed footer icon shadow 太具体

当前状态:

`.ui-feed-footer-state .ui-state-icon` 使用:

```text
0 8px 24px color-mix(...)
```

问题:

这是少量非 token shadow，且 footer 状态不应比内容更有存在感。对标 Threads/X，feed footer 应非常安静。

建议:

改为状态 depth token，例如:

```text
--ui-state-feed-icon-shadow
```

默认可以是 none 或 extremely subtle。

### P1-04 `Skeleton` 允许任意 inline width/height

当前状态:

基础 `Skeleton` 接收 `width`、`height` 并写到 inline style。页面级 skeleton 已经大量走类名 preset，这是好方向。

问题:

escape hatch 合理，但如果页面继续传裸数字，会削弱 skeleton preset。

建议:

Release 1 不必先改 `Skeleton` API，但需要在实施门禁中要求页面级 skeleton 只用 preset class。

## 可以直接开工的最小文件集

第一组: 状态 API 和样式基础

- `src/ui/UIStateBlock.tsx`
- `src/ui/LoadingState.tsx`
- `src/ui/EmptyStateCard.tsx`
- `src/styles/tokens/foundation.css`
- `src/styles/02-core-controls.css`
- `src/styles/system/ui-primitives-feedback.css`

第二组: 首页应用

- `src/features/home/HomeDesktopFeedContent.tsx`
- `src/features/feed/DouyinFeed.tsx`
- `src/styles/features/home-feed-foundation.css`
- `src/styles/system/feed-scroll-shell.css`

第三组: 登录墙 API 和样式基础

- `src/ui/AuthRequiredState.tsx`
- `src/styles/system/ui-primitives-auth.css`

第四组: 登录墙调用迁移

- `src/features/post-create/PostCreatePage.tsx`
- `src/features/profile/ProfileMobilePage.tsx`
- `src/features/promote/PromoteMobilePage.tsx`
- `src/pages/RechargeMobile.tsx`
- `src/features/sponsor/SponsorMobilePage.tsx`
- `src/pages/PromoteHistory.tsx`
- `src/App.tsx`

## 暂时不要先碰的范围

不要在 Release 1 一开始就改:

- Admin 页面。
- Promote payment sheet。
- Profile dialog。
- Media/lightbox/upload 细节。
- PageContentShell desktop split。
- 全站 typography token 大改。

原因:

这些属于 Release 2 到 Release 5。Release 1 应先把 state 和 auth 合同立住，否则后面会边改边漏。

## 推荐实施顺序

1. 在 `UIStateBlock` 和 `StateBlock` 增加 `placement`、`density`，兼容旧 `compact`、`inline`。
2. 在 foundation 增加 state placement token。
3. 在 `02-core-controls.css` 增加 `data-state-placement` 或 modifier class，先实现 `page`、`feed`、`panel`、`inline`。
4. 改 `EmptyStateCard`，移除 `py-12`、`py-16`。
5. 改 `HomeDesktopFeedContent`，用 `placement="feed"` 替换 `surface-card home-desktop-error-state` 的核心视觉职责。
6. 改 `DouyinFeed`，初始空/错状态用 feed placement，footer 用 load-more/footer placement。
7. 在 `AuthRequiredState` 增加 `placement`、`context`、`density`、`preview`、`meta`、`secondaryAction`。
8. 在 `ui-primitives-auth.css` 用 data attributes 实现 context-aware 轻量状态。
9. 迁移 create/profile/promote 三个高影响登录墙。
10. 再迁移 recharge/sponsor/promote history/App fallback。

## Release 1 验收门槛

视觉:

- 首页错误态不再像孤立卡片。
- 首页空态、错误态、加载更多有同一 feed state 语言。
- 发布、个人、推广未登录态不再像同一张卡复制。
- 常规状态默认 flat 或 hairline，不默认 shadow。

布局:

- feed 状态不超过 feed 内容宽度。
- 移动端状态不被底部导航遮挡。
- only-child CSS 不再承担初始状态布局语义。
- 页面不再通过 `.post-create-page .ui-auth-required-card` 这类方式决定登录墙核心视觉。

字体:

- 状态标题、说明、action 的层级由 token 控制。
- 描述文案更轻，不和标题抢权重。

颜色:

- error、empty、info、success tone 有清楚但克制的区别。
- 状态不只靠颜色表达。

间距:

- 状态 min-height、padding、action gap 都来自 placement/density token。
- `EmptyStateCard` 不再拼 `py-*`。

工程约束:

- 不新增裸视觉数字。
- 不新增页面级强压选择器。
- 不新增每页自己的 login card 或 empty card。
- 旧 props 兼容，避免一口气破坏所有调用点。

## 本阶段结论

Release 1 的代码基础已经存在，最正确的做法不是重写状态组件，而是给现有 `UIStateBlock`、`StateBlock`、`AuthRequiredState` 补足 placement、density、context 和 slot 合同。这样可以直接解决首页失败态、通用登录墙、状态卡过重这三个当前最影响精致感的问题，同时避免继续通过页面 CSS 强压来修局部像素。

目标仍未完成。Phase 12 只证明 Release 1 可以安全开工，并明确了最小改动路径；完整目标还需要后续代码落地、截图回归、动态验证和 Phase 11 门禁回填。

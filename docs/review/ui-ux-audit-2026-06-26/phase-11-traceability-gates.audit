# UI/UE 深度审计 Phase 11 - 全目标追踪门禁

日期: 2026-06-26

目标: 将原始目标拆成可追踪、可验收、不可偷换范围的门禁表。这个文件解决一个关键风险: 后续即使修了几个显眼页面，也不能因此误判为“所有页面、所有组件、所有布局、所有字体、所有颜色、所有间距”已经完成。

本阶段定位: 范围追踪和完成门禁。它不替代 Phase 9 的截图采集协议，不替代 Phase 10 的实施 backlog，而是给两者加上完成判定规则。

## 原始目标到门禁的映射

### 门禁 1: 所有页面

必须证明:

1. 15 个主路由都有移动截图。
2. 15 个主路由中核心用户路由都有桌面截图。
3. 直接访问和 overlay 进入的页面不能混为一谈。
4. 登录态、未登录态、空态、错误态、真实内容态必须按页面语义记录。

当前状态:

- 已有截图: `/`、`/create`、`/chat`、`/profile`、`/promote`、`/about`。
- 缺截图: `/recharge`、`/transactions`、`/sponsor`、`/post/:id`、`/user/:id`、`/promote/history`、`/404`、`/168wc`、`/category/:categoryId`。
- 缺 overlay: `/post/:id`、`/user/:id`、`/category/:categoryId`、`/about` 从首页进入。

不得宣称完成的情况:

- 只有未登录截图。
- 只有移动截图。
- 只有直接访问截图，没有 overlay 进入截图。
- 只有空态，没有真实内容态。

### 门禁 2: 所有组件

必须证明:

1. 顶层 30 个 UI 组件都有合同归属。
2. 高杠杆组件不仅要读代码，还要覆盖关键状态。
3. 组件状态至少包含 default、hover 或 active、focus、disabled、loading、error、empty、long content 中相关状态。
4. 组件不能靠页面 className 推断语义。

当前已具备强证据的组件家族:

- State: `UIStateBlock`、`AuthRequiredState`、`EmptyStateCard`、`LoadingState`、`ListLoadMoreState`、`ErrorBoundary`。
- Action: `ActionButton`、`IconButton`、`TopbarIconButton`、`PublishIconButton`、`ProfileIconButton`、`TelegramContactIconButton`。
- Media: `OptimizedImage`、`AvatarImage`、`ImageLightbox`、`PostMediaGrid`、`ImageUpload`。
- Layout: `AppPage`、`PageContentShell`、`PageHeader`、`SurfaceSectionCard`、`SegmentTabs`。
- Sheet/Form: `BottomSheet`、`SettingRow`、`HeaderSelectAction`、profile dialog、home structured filter、promote payment sheet。

仍需补强:

- Admin 子组件。
- Chat 子模块。
- Profile security and edit 子状态。
- Post quote and Telegram sync confirm。
- Hover/focus/disabled/busy 的实际截图或动态证据。

不得宣称完成的情况:

- 只读了组件 TSX，没有看对应 CSS。
- 只看了默认状态，没有看 loading/error/disabled。
- 只看了 mobile，没有看 desktop 或窄屏。

### 门禁 3: 所有布局

必须证明:

1. 每个页面都归属一个清晰 composition: feed、editor、readable、split、wide、fluid、modal、overlay。
2. 桌面留白必须变成信息结构，而不是移动壳居中。
3. 容器负责外部布局，子组件只负责内部内容。
4. toolbar、action row、sheet、dialog 都有单一布局所有者。

当前关键风险:

- Chat desktop 像手机壳居中。
- About desktop 像移动卡片放大。
- Auth/login wall 在桌面形成巨大卡片孤岛。
- Post create toolbar 曾经暴露容器和按钮双重宽度合同。
- Profile dialog 有父级强控子元素宽度的风险。

验收证据:

- 390 x 844。
- 1280 x 720。
- 1440 x 900，对 desktop composition 必测。
- 360 x 740，对工具栏、sheet、支付、键盘相关页面必测。

不得宣称完成的情况:

- 只验证移动端。
- 桌面只是把 mobile max-width 拉宽。
- sheet 打开时没有验证 safe area、滚动、footer、键盘。

### 门禁 4: 所有字体

必须证明:

1. Page title、section title、card title、body readable、label、meta 都有语义 token。
2. 中文长文、长地点、长用户名、英文 URL、Telegram handle、金额数字都被验证。
3. 字体权重服务层级，不靠加粗制造重要性。
4. 截断、省略、换行、行高有明确规则。

当前关键风险:

- `UIText` variant 过少。
- 状态页标题和说明层级偏硬。
- 关于页和登录墙的说明文字不够轻。
- Feed 内容截断行数在 TSX 常量里表达。
- 部分 admin 字号使用 utility 或裸字号。

验收证据:

- 短中文。
- 长中文。
- 中英混排。
- URL。
- 金额和多位计数。
- 超长用户名和地点。

不得宣称完成的情况:

- 只看视觉截图，没有验证极端文本。
- 只看标题，没有看 body/meta。
- 只看 C 端，没有记录 admin 是否另起子主题。

### 门禁 5: 所有颜色

必须证明:

1. Page surface、card surface、muted surface、overlay surface、brand surface、error surface 都有语义。
2. 默认页面不靠白卡片和阴影制造层级。
3. muted text、disabled、error、brand button、white-on-overlay 经过对比度检查。
4. 状态不能只靠颜色表达。

当前关键风险:

- 白卡片占比偏高。
- filter chip 和 sheet shadow 偏厚。
- avatar fallback 视觉强度偏高。
- lightbox close control 白色材料和 shadow 偏强。
- muted text 在 soft surface 上可能不足。

验收证据:

- Light surface。
- Muted surface。
- Overlay surface。
- Error state。
- Disabled state。
- Brand action。

不得宣称完成的情况:

- 只做主观视觉判断，没有记录对比度。
- 只看正常态，没有看 error/disabled/selected。
- 图片背景上白字没有测。

### 门禁 6: 所有间距

必须证明:

1. Page rhythm、section gap、cluster gap、control gap、card padding、sheet padding 都有 token 或合同归属。
2. 不同页面不能各自发明 spacing。
3. 容器间距和组件内部间距不能互相覆盖。
4. 窄屏不能通过隐藏核心信息来规避重叠。

当前关键风险:

- `BrandAbout.tsx`、`NotFound.tsx`、`ImageUpload.tsx`、`PromotePaymentSheet` 中存在 utility spacing 风险。
- Admin utility spacing 债巨大。
- `@media (max-width: 390px)` 分散。
- Sheet、dialog、profile 子选择器存在强压风险。

验收证据:

- 390 x 844 normal。
- 360 x 740 compact。
- 1280 x 720 desktop。
- Open sheet。
- Keyboard open。
- Long content。

不得宣称完成的情况:

- 只看默认数据。
- 只测一个 viewport。
- 仍存在新增页面级强压选择器。

### 门禁 7: 精致感、呼吸感、简约感

必须证明:

1. 内容优先，chrome 不抢戏。
2. 页面有空气，但空气有结构。
3. 默认 surface 轻，特殊状态才重。
4. 操作入口稳定、克制、命中区清楚。
5. 动效和状态反馈服务理解，不制造噪声。

当前已识别的主要阻碍:

- 首页失败态。
- 通用登录墙。
- 桌面手机壳。
- 卡片和阴影过多。
- Icon action 分裂。
- Media fallback 和 placeholder 分裂。
- Sheet 和 payment 弹层不统一。

不得宣称完成的情况:

- 只是调了颜色或圆角。
- 只是减少内容密度但没有增强结构。
- 只在一个页面达成高级感，其他页面仍是旧合同。

### 门禁 8: 不硬编码、不临时方式、不 CSS 强压

必须证明:

1. 新增视觉值进入 token 或命名常量。
2. 新增页面不写共享组件内部覆盖。
3. 新增组件 API 用显式 prop，不用 className 字符串推断。
4. 视觉 JSX 中裸数字显著下降。
5. Admin 如纳入目标，必须单独治理 utility 债。

当前关键风险:

- C 端 TSX utility/硬编码风险约 84 处。
- Admin TSX utility/硬编码风险约 522 处。
- 非 token CSS px/hex/rgba 命中约 91 处。
- `ProfileIconButton`、`TopbarIconButton`、`SegmentTabs`、`PromotePaymentSheet` 等存在合同逃逸点。

不得宣称完成的情况:

- 用更复杂的 CSS 盖住旧 CSS。
- 通过 `!important`、Tailwind `!`、父选择器通配子元素修复。
- 在 TSX 中继续传裸 icon size、skeleton width、animation duration。

### 门禁 9: 对标 X、Threads、Instagram、Apple

必须证明:

1. Feed 对齐 X/Threads 的内容优先和轻分隔。
2. Profile 和 media 对齐 Instagram 的身份层级和媒体稳定性。
3. Surface、motion、typography 对齐 Apple 的克制材料、清晰层级和低噪声反馈。
4. Benchmark 不是口号，必须映射到具体页面和组件。

当前已具备原则映射:

- Feed card 已接近 Threads/X 的轻边线方向。
- Profile tab 有 Instagram 式 underline 方向。
- Apple 式克制被转译为 SurfaceDepth、StatePlacement、Typography、SheetContract。

仍缺:

- 逐屏 side-by-side benchmark。
- 当前同类产品截图证据。
- Apple HIG 逐条映射。

不得宣称完成的情况:

- 只说“像 Apple 一样简洁”。
- 只借鉴颜色、圆角、动效，不映射信息层级。

## 路由门禁表

### P0 路由

`/`

- 必须补: 已登录真实 feed、错误态、loading、空态、移动和桌面。
- 合同依赖: FeedStateContract、PageContentShell、MediaContract。
- 完成证据: 内容流在失败和真实数据下都保持结构。

`/create`

- 必须补: 已登录编辑器、长文本、1 图、9 图、上传失败、分类字段、地点、隐私、联系方式。
- 合同依赖: AuthRequiredState、ComposerToolbar、CreatorField、MediaContract、SheetContract。
- 完成证据: 工具栏摘要稳定，字段轻量，发布任务语义清楚。

`/profile`

- 必须补: 已登录资料、帖子列表、空列表、编辑 dialog、移动和桌面。
- 合同依赖: AuthRequiredState、Identity action、SegmentTabs、SheetContract。
- 完成证据: 身份资产层级清楚，不像通用登录卡。

`/post/:id`

- 必须补: 纯文本、单图、多图、长文、错误/删除态、overlay 进入。
- 合同依赖: Feed typography、MediaContract、ActionRow、Overlay contract。
- 完成证据: 内容详情是主角，媒体和动作不抢正文。

`/promote`

- 必须补: 选帖子、配置、checkout、支付确认、成功/失败态、移动键盘。
- 合同依赖: AuthRequiredState、CommerceFlow、SheetContract、CreatorField。
- 完成证据: 高价值流程可信、清楚、不拥挤。

### P1 路由

`/chat`

- 必须补: 真实会话、长消息、输入框键盘、桌面 split。
- 合同依赖: PageContentShell split、Chat shell、IconAction。
- 完成证据: 桌面不再是手机壳，移动输入稳定。

`/recharge`

- 必须补: 金额输入、二维码、复制地址、错误态。
- 合同依赖: CommerceFlow、SurfaceDepth、SheetContract。
- 完成证据: 金融流程可信但不重。

`/transactions`

- 必须补: 有记录、空记录、加载失败。
- 合同依赖: RecordList、StatePlacement、Typography。
- 完成证据: 金额、时间、状态、ID 可扫描。

`/sponsor`

- 必须补: 赞助中心、可赞助列表、空态、确认态。
- 合同依赖: CommerceFlow、AuthRequiredState、RecordList。
- 完成证据: 价值和记录结构清楚。

`/user/:id`

- 必须补: 用户资料、帖子、空态、关注/联系、overlay 进入。
- 合同依赖: Identity action、Profile layout、MediaContract。
- 完成证据: 用户身份和内容关系清楚。

`/promote/history`

- 必须补: 有历史、空历史、状态、操作。
- 合同依赖: CommerceFlow、RecordList、StatePlacement。
- 完成证据: 历史记录不堆卡片，可快速扫描。

`/about`

- 必须补: desktop editorial 回归、mobile reading 回归、overlay 进入。
- 合同依赖: PageContentShell readable/wide、Typography、SurfaceDepth。
- 完成证据: 桌面不是移动卡片放大，叙事连续。

`/category/:categoryId`

- 必须补: 真实列表、空列表、加载更多、overlay 进入。
- 合同依赖: FeedState、Category header、ActionRow。
- 完成证据: 分类发现和首页 feed 同属一套语言。

### P2 路由

`/404`

- 必须补: 移动和桌面。
- 合同依赖: UIStateBlock placement page。
- 完成证据: 404 使用统一状态系统，不写局部 min-height/gap。

`/168wc`

- 必须补: admin desktop、admin mobile、table、card、form、empty。
- 合同依赖: AdminDesignDebtContract。
- 完成证据: 若纳入全站目标，admin 不能继续由 TSX utility 主导视觉。

## 组件家族门禁表

State family:

- 覆盖: `UIStateBlock`、`AuthRequiredState`、`EmptyStateCard`、`LoadingState`、`ListLoadMoreState`、`ErrorBoundary`。
- 必须证明: page/feed/panel/inline placement 清楚。
- 不得出现: 每页自建 empty/auth/error card。

Action family:

- 覆盖: button、icon button、topbar action、publish、profile、telegram、sheet close、lightbox close。
- 必须证明: size、stroke、tone、shape、label 统一。
- 不得出现: icon `size={17}` 这种视觉裸数字。

Media family:

- 覆盖: optimized image、avatar、post media、upload、lightbox、profile cover。
- 必须证明: preset、fallback、loading、error、motion 统一。
- 不得出现: 坏图静默消失或品牌 placeholder 刷屏。

Field family:

- 覆盖: 发布字段、筛选字段、picker、segmented、range、setting row。
- 必须证明: label、hint、value、error、selected 同一语言。
- 不得出现: 发布页和筛选页像两套控件。

Sheet family:

- 覆盖: bottom sheet、filter sheet、payment sheet、profile dialog、lightbox immersive layer。
- 必须证明: header、body、footer、close、motion、focus、keyboard 策略清楚。
- 不得出现: sheet 内 utility animation 和局部 padding 主导视觉。

Shell family:

- 覆盖: AppPage、PageContentShell、PageHeader、SurfaceSectionCard、SegmentTabs。
- 必须证明: 页面 composition 和 surface depth 清楚。
- 不得出现: 桌面手机壳和卡片岛。

## 完成证据包格式

每完成一个页面或合同，必须补一条记录:

```text
Scope:
Files or routes:
Design dimension covered:
Screenshots:
Dynamic checks:
Hardcoding check:
Benchmark mapping:
Open risks:
Completion verdict:
```

`Completion verdict` 只能是:

- `proved`
- `partially proved`
- `not proved`

禁止使用:

- `looks good`
- `probably done`
- `covered enough`
- `no obvious issue`

## 最小完成门槛

原始目标最小完成门槛:

1. Phase 9 Batch A 到 E 的核心截图存在。
2. Phase 10 Release 1 的 P0 状态体系完成并回归。
3. IconAction 和 MediaContract 基础完成。
4. 发布页、首页、详情页、资料页、推广页、聊天页均有移动和桌面可接受证据。
5. 字体、颜色、间距各有独立验证记录，而不是混在页面主观描述里。
6. 动态验证至少覆盖 keyboard、focus、dialog、mobile keyboard、long text、image failure、contrast。
7. 硬编码风险有新的当前状态证据证明下降。

如果以上任一条缺失，目标保持进行中。

## 本阶段结论

Phase 11 明确了“什么证据才能证明目标完成”。当前目标仍未完成，但范围已经从宽泛愿望收敛成可追踪门禁: 页面门禁、组件门禁、布局门禁、字体门禁、颜色门禁、间距门禁、风格门禁、工程约束门禁和 benchmark 门禁。后续每次实施或补截图，都应回填这些门禁，而不是只凭局部视觉改善判断完成。

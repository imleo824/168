# UI/UE 深度审计 Phase 5 - Feature 级业务模块系统审计

日期: 2026-06-26

目标: 从真实业务模块出发，审计首页信息流、帖子卡片、媒体、筛选、发布、推广、充值、赞助、资料、用户空间、后台等 feature 层的视觉和体验债。重点保持三条约束: 不硬编码、不临时方式、不靠 CSS 强压。

## 本阶段深读范围

Feature TSX:

1. `src/features/post/PostCard.tsx`
2. `src/features/post/PostMediaGrid.tsx`
3. `src/features/home/HomeFeedContent.tsx`
4. `src/features/home/HomeStructuredFilterSheet.tsx`
5. `src/features/post-create/postCreateComponents.tsx`
6. `src/features/promote/promoteComponents.tsx`
7. `src/features/profile/ProfileHeaderCover.tsx`
8. `src/features/social/FollowButton.tsx`
9. `src/features/upload/ImageUpload.tsx`

Feature and component CSS:

1. `src/styles/components/feed-card-shell.css`
2. `src/styles/components/feed-card-content.css`
3. `src/styles/components/feed-card-footer.css`
4. `src/styles/components/media.css`
5. `src/styles/features/home-feed-foundation.css`
6. `src/styles/features/home-structured-filters.css`
7. `src/styles/features/create-promote-post-editor.css`
8. `src/styles/features/create-promote-post-settings.css`
9. `src/styles/features/promote-layout-checkout.css`
10. `src/styles/features/profile-modern.css`
11. `src/styles/features/recharge.css`
12. `src/styles/features/sponsor.css`
13. `src/styles/features/admin.css`
14. `src/styles/features/user-space-next.css`
15. `src/styles/components/profile-dialog.css`

## 总体判断

- 内容流和帖子卡片是当前最接近目标的模块。它们已经从重卡片转向轻边线列表，方向接近 X/Threads。
- 发布、筛选、推广、支付等复杂模块虽然 token 使用不少，但局部 utility、具体数值和临时 className 仍明显更多。
- 商业流程和后台最容易变重: 卡片、阴影、sticky checkout、sheet、支付面板叠加后，精致感容易变成“控件很多但很忙”。
- 要继续提升全站质感，不能按页面零敲碎打，而要按 feature 家族统一合同: feed content、media、filter sheet、creator tools、checkout sheet、record list、identity profile。

## P0: 首页信息流和帖子卡片

### 优点

- `feed-card-shell.css` 中 `.ins-post-card` 使用 `border-bottom`、无阴影、无圆角、轻背景，方向接近 Threads/X 的内容流。
- 作者行、头像列、主体列、动作区都已经大量 token 化，说明基础设计系统可用。
- `PostCard.tsx` 中互动保护、预取、点击穿透、防嵌套交互误触等工程处理较成熟。

### 问题

1. 帖子卡片 TSX 仍有内容显示硬常量
证据: `CONTENT_WITH_MEDIA_CLAMP_LINES = 4`、`CONTENT_TEXT_ONLY_CLAMP_LINES = 6`、`POST_DETAIL_PREFETCH_DELAY = 120`。
影响: 文本截断行数是视觉节奏的一部分，不应散在 TSX。
建议: 抽到 feed typography/content token，例如 `--ui-feed-card-media-preview-lines`、`--ui-feed-card-text-preview-lines`，TSX 只声明内容类型。

2. 帖子动作区五等分和六等分设计需要更严格响应式证明
证据: `feed-card-footer.css` 使用 `repeat(5, minmax(0, 1fr))` 和 telegram sync 下 `repeat(6, minmax(0, 1fr))`。
影响: 窄屏、多数字、Telegram 同步、联系按钮同时出现时容易压缩，和之前发布页工具栏摘要问题同类。
建议: 建立 `ActionRow` 组件合同，容器管理列数，按钮只管理内部图标与数字；长数字可走 compact formatting。

3. 热度 icon 有微调痕迹
证据: `.feed-action-heat-icon { transform: translateY(-1px); }`。
影响: 这种视觉对齐如果散落多个 icon，会形成微妙漂移。
建议: 统一 icon baseline token，不在单个 icon 上做局部位移。

4. 头像 fallback 视觉很强
证据: `media.css` 中 `.ui-avatar-fallback` 使用多层径向渐变、金色文字、宋体 fallback。
影响: 作为默认用户头像，装饰感可能超过内容本身，不够 Threads/X 式克制。
建议: 头像 fallback 增加 `neutral` 版本，默认内容流使用低噪声 neutral，品牌场景才使用 gold/brand。

### 优先建议

P0. 信息流失败态必须内联化，不能中断列表框架。
P1. 帖子动作区改为统一 `FeedActionRow` 合同。
P1. 头像 fallback 降噪。

## P0: 媒体和图片体验

### 优点

- `PostMediaGrid` 支持最多 9 图、隐藏数量、手势拖动保护、动态比例、图片加载状态，媒体体验基础扎实。
- `media.css` 已经把 media frame、carousel、count badge、loading placeholder、avatar 统一到组件 CSS。

### 问题

1. 媒体组件存在 TSX 视觉字符串
证据: `MEDIA_IMAGE_CLASS = 'ui-media-image h-full w-full object-cover object-center'`、`MEDIA_TILE_BASE_CLASS` 含 `relative block overflow-hidden touch-manipulation ...`。
影响: 媒体核心布局仍部分由 utility 字符串控制。
建议: 全部收敛到 `.ui-media-image`、`.media-carousel-slide` 等 CSS 类。

2. 媒体比例和手势阈值散在 TSX
证据: `CAROUSEL_DRAG_THRESHOLD = 12`、`MIN_CAROUSEL_ASPECT_RATIO = 3 / 4`、`MAX_CAROUSEL_ASPECT_RATIO = 4 / 3`、`DEFAULT_CAROUSEL_ASPECT_RATIO = '16 / 9'`。
影响: 这些是媒体体验合同，不应每个组件单独发明。
建议: 抽为 media behavior constants，并在 CSS token 中映射比例。

3. 媒体加载占位文案太品牌化
证据: `MEDIA_PLACEHOLDER_TEXT = '圈内事 发推推'`。
影响: 多图流里重复出现品牌文案，会增加噪声。
建议: feed 图片占位改为无文字或极弱视觉图形；品牌文案只在空态/错误态出现。

4. 上传进度环有裸数字
证据: `ProgressRing({ pct, size = 36 })`，`strokeWidth={3}`，`style={{ transition: 'stroke-dashoffset 0.2s ease' }}`。
影响: 发布编辑器里的进度动效和尺寸没有走 motion/control token。
建议: 用 CSS custom properties 控制 ring size、stroke、duration、easing。

## P0: 发布编辑器和创作工具

### 优点

- 发布页工具栏已经建立 grid 合同，并且修复过摘要被挤没的问题。
- 分类结构化字段有较完整的控件拆分: 文本、数字、boolean、select、location。
- `post-create-settings` 样式大多 token 化，语义清楚。

### 问题

1. 发布工具栏还有历史遗留的二次宽度合同
证据: `.post-create-tool-button, .image-upload-toolbar-trigger` 中仍设置 `width: var(--post-create-tool-button-width)` 和 `flex: 0 0 var(...)`，随后又通过 `.post-create-tool-row > ... { width: 100%; flex-basis: 100%; }` 覆盖。
影响: 虽然当前修复可用，但合同仍显复杂，后续容易再次踩坑。
建议: 删除按钮自身的五等分宽度语义，只保留 toolbar container 是布局所有者。

2. 分类选项存在非 token 尺寸
证据: `.post-create-category-option { min-height: 3rem; }`。
影响: 这是典型设计系统逃逸点。
建议: 改为 `--post-create-category-option-min-height`，放到 create token。

3. 发布组件中部分 icon/输入缺少统一 Action/Input primitive
证据: `CategoryMetaFieldRow` 内 select/location picker、boolean segmented、number input 都直接写具体类。
影响: 发布页内部控件会和首页筛选 sheet、推广 sheet 的控件长得类似但细节不同。
建议: 建立 `FormFieldRow`、`SegmentedControl`、`PickerButton` 三个共享 primitive。

4. 上传组件和发布工具栏职责混杂
证据: `ImageUpload` 同时支持 grid 和 toolbar layout，内部控制上传、预览、错误、trigger 摘要。
影响: 复杂度高，toolbar 场景容易影响 grid 场景。
建议: 保留 upload core，拆 `ImageUploadGrid` 和 `ImageUploadToolbarTrigger` 外壳。

## P1: 首页结构化筛选

### 优点

- `HomeStructuredFilterSheet` 的字段识别、地点分组、范围校验、摘要构建都比较完整。
- `home-structured-filters.css` 对 sheet、chips、location、range、actions 都有清晰样式归属。

### 问题

1. 筛选 sheet 视觉比内容流重
证据: `.home-structured-filter-sheet` 有顶部边框、渐变背景、大上投影。
影响: 筛选作为辅助操作，不应比内容流更“厚”。
建议: 降低 sheet shadow，使用 hairline + material 背景，减少黑色 18% 阴影。

2. 筛选 chips 过多使用 shadow
证据: `.home-structured-filter-chip`、`.home-structured-location-option`、`.home-structured-segment` 都有轻微 box-shadow。
影响: 大量 chips 同屏时会显得密和碎。
建议: 默认 flat，选中态用色块/边框，不用 shadow。

3. 地点列表高度使用 `min(42vh, 22rem)`
影响: 这是功能合理值，但仍是局部合同。
建议: 抽为 `--home-structured-location-grid-max-height`。

4. 价格范围和地点控件与发布页字段控件重复
建议: 与发布页共享 `FieldOptionGrid`、`RangeField`、`LocationPickerList` 样式/组件合同。

## P1: 推广和支付流程

### 优点

- 推广 checkout bar 对键盘、safe area、底部 fixed 有较完整处理。
- `PromotePaymentSheet` 关注输入框聚焦可见性，说明交互细节意识很好。

### 问题

1. 推广支付面板含大量 TSX utility 和动画强绑定
证据: `className="ui-sheet-panel promote-payment-sheet relative max-w-md animate-[sheet-up-in_200ms_ease-out] space-y-4 overflow-y-auto p-5"`。
影响: 这是明显违背“不硬编码、不临时方式”的风险点。
建议: 建立 `.promote-payment-sheet` 完整 CSS 合同，motion 使用 `--ui-duration-*` 和 `--ease-*`。

2. 支付可视区域修正使用裸数字
证据: `topLimit = ... + 16`、`bottomLimit = ... - 24`、`PAYMENT_FOCUS_STABILIZE_DELAYS = [120, 280, 460]`。
影响: 这些属于键盘/输入可见性行为合同。
建议: 抽为 `PAYMENT_FOCUS_TOP_INSET`、`PAYMENT_FOCUS_BOTTOM_INSET`、`PAYMENT_FOCUS_STABILIZE_DELAYS` 常量，并绑定 token 命名。

3. checkout bar 栅格存在百分比魔法
证据: `grid-template-columns: minmax(0, 1fr) minmax(var(--promote-checkout-action-min-width), 36%)`。
影响: 36% 是布局判断，不够语义化。
建议: 抽为 `--promote-checkout-action-column-max`。

4. sheet、picker、payment 没有统一 BottomSheet 合同
证据: promote picker 使用 `BottomSheet`，payment 自己实现 overlay/dialog。
影响: 两种弹层视觉和可访问性可能不一致。
建议: 将 payment sheet 迁移到增强后的 `BottomSheet` 或建立统一 `ModalSheet`。

## P1: 充值、赞助、记录类页面

### 优点

- `recharge.css` 和 `sponsor.css` 的 token 化程度较高。
- 充值页有较清晰的金额、估算、二维码、地址、复制动作结构。
- 赞助页使用列表和底部边线，部分地方已经接近轻量后台/钱包体验。

### 问题

1. 充值页卡片仍偏多
证据: balance card、form card、payment card 全是 border + background + optional shadow。
影响: 金融/充值流程需要信任，但过多卡片会变沉。
建议: 余额区做轻 hero，表单和支付步骤用连续 section，而不是所有内容都放卡片。

2. 充值二维码尺寸使用局部表达
证据: `--ui-recharge-qr-size: min(64vw, calc(var(--ui-space-8) * 7))`。
建议: 如果二维码尺寸是全站支付资产，应抽为 payment/qr token。

3. 赞助 skeleton 尺寸使用百分比
证据: `.sponsor-row-skeleton-title { width: 42%; }`、`.sponsor-row-skeleton-meta { width: 64%; }`。
影响: 可以接受，但最好统一到 skeleton line presets。
建议: 使用 `--ui-skeleton-line-*` token。

4. `@media (max-width: 390px)` 分散
证据: promote、profile、sponsor 等都有 390px 特判。
影响: 设备断点合同分散，后续维护容易混乱。
建议: 建立 `--ui-compact-viewport` 或统一 responsive layer。

## P1: 资料和用户空间

### 优点

- profile tabs 已经采用轻边线和底部 underline，方向接近 Instagram profile。
- `ProfileHeaderCover` 独立封装，用户空间 actions 有 split/single layout 语义。

### 问题

1. Profile cover sizes 是硬编码字符串
证据: `PROFILE_HEADER_COVER_SIZES = '(max-width: 48rem) 100vw, 72rem'`。
建议: 抽为 profile media preset。

2. profile tabs 依赖全局 `SegmentTabs` 但样式在 `.profile-modern-page` 大量覆盖
影响: 这是组件 API 不足导致的上下文覆盖。
建议: `SegmentTabs` 增加 `variant="underline|pill|compact"`，profile 使用 underline variant。

3. user-space state block padding 偏硬
证据: `.user-space-state-block { padding-block: calc(var(--ui-space-8) * 3); }`。
影响: 这是页面局部空态距离，应该归入 state placement。
建议: `UIStateBlock placement="page"` 管理。

4. Profile dialog 内部有较多宽度 calc 和子选择器控制
证据: `.profile-dialog-panel > :not(...) { width: calc(100% - (...)) }`。
影响: 容器对子元素做强约束，长期会变成 CSS 强压。
建议: dialog body/header/actions 显式结构化，避免通配子选择器管理布局。

## P2: 后台 Admin

### 优点

- `admin.css` 已经开始补 admin token，如 sidebar、panel、nav、card。
- 桌面 sidebar 和移动 tabstrip 的方向是可维护的。

### 问题

1. Admin TSX 仍是最大硬编码债
证据: Phase 3 静态扫描显示 `AdminPage.tsx` 有大量 `text-[10px]`、`px-*`、`py-*`、`bg-gray-*`、`rounded-*`。
影响: 即使 `admin.css` token 化，TSX 内 utility 仍会主导最终像素。
建议: Admin 单独 Phase，先抽 `AdminBadge`、`AdminTable`、`AdminActionButton`、`AdminMetricCard`。

2. admin.css 自身仍有裸尺寸
证据: `.admin-user-list-avatar` 写 `32px`、mobile 写 `36px`、glyph 写 `0.82rem`、`.admin-mobile-name { max-width: 10ch; }`。
建议: 统一为 admin avatar token 和 text clamp token。

3. Admin 视觉方向和 C 端可不同，但 token 源不能割裂
建议: 建立 admin 子主题，继承 foundation tokens，但允许 admin 独立 density。

## Feature 层硬编码风险清单

P0:

- `ImageUpload` 的 progress ring size/stroke/transition。
- `PromotePaymentSheet` 的 utility animation、spacing、max width、focus inset 数字。
- `PostCreate` toolbar 的按钮宽度双重合同。

P1:

- `PostMediaGrid` 的媒体比例、drag threshold、placeholder 文案。
- `HomeStructuredFilterSheet` 的 sheet shadow、chip shadow、location max-height。
- `ProfileHeaderCover` 的 sizes 字符串。
- `SegmentTabs` 在 profile 下被大量上下文覆盖。

P2:

- `AdminPage` 大量 Tailwind utility。
- admin avatar 32px/36px。
- sponsor skeleton 百分比宽度。
- 多处 390px compact breakpoint。

## 对标 X / Threads / Instagram / Apple 的关键差距

1. 内容流
当前方向接近 Threads，但错误态和加载态还不够内联，失败时内容流断掉。

2. 媒体
Instagram 的媒体体验有一致的比例、边距、预览、沉浸查看。当前媒体能力强，但 placeholder、fallback、lightbox、upload 仍没有统一 media contract。

3. 发布
X/Threads 的发布器更像轻量输入层，工具入口服务内容。当前发布器功能强，但工具栏和结构化字段很容易变成表单后台，需要继续降低控件存在感。

4. 筛选
顶级社交产品的筛选通常是轻量入口，只有必要时展开复杂面板。当前结构化筛选能力强，但 sheet/chip 视觉偏厚。

5. 商业流程
Apple 式支付/确认流程强调少量信息、清晰动作、稳定底栏。当前推广支付功能细，但样式合同不统一，容易显忙。

## 下一轮建议: Phase 6 实施前设计合同

1. `FeedStateContract`
覆盖: 首页空态、错误态、加载更多、列表失败、帖子 skeleton。

2. `MediaContract`
覆盖: PostMediaGrid、OptimizedImage、AvatarImage、ImageUpload、ImageLightbox、ProfileHeaderCover。

3. `CreatorFieldContract`
覆盖: 发布页分类字段、首页结构化筛选、设置行、picker button、segmented control。

4. `SheetContract`
覆盖: BottomSheet、HomeStructuredFilterSheet、PromotePickerSheet、PromotePaymentSheet、ProfileDialog。

5. `CommerceFlowContract`
覆盖: Promote checkout、Recharge QR、Sponsor record、Transaction history。

6. `AdminDesignDebtContract`
覆盖: Admin badge/table/action/card/mobile list。

## 本阶段结论

内容流基础已经走在正确方向，真正的下一步不是“美化每个页面”，而是把 feature 层的重复控件收敛到少数合同。建议后续优先实施:

1. `FeedStateContract`，修首页失败态和列表状态。
2. `MediaContract`，统一图片、头像、上传、lightbox。
3. `CreatorFieldContract`，统一发布和筛选字段。
4. `SheetContract`，统一复杂弹层和支付面板。

目标仍未完成，因为本阶段是静态 feature 审计，缺真实登录态、真实内容、完整流程截图和可达性验证。

# UI/UE 深度审计 Phase 18 - 媒体与身份层级专项审计

日期: 2026-06-26

目标: 补齐详情页、个人页、用户空间和媒体组件的像素级审计。此阶段关注 Instagram 式媒体和身份层级，同时借鉴 X/Threads 的低噪声 action row，以及 Apple 的清晰、克制、可预期原则。

## 本阶段读取证据

页面:

- `src/pages/PostDetail.tsx`
- `src/features/profile/ProfileMobilePage.tsx`
- `src/pages/UserSpace.tsx`

组件:

- `src/features/post/PostMediaGrid.tsx`
- `src/ui/ImageLightbox.tsx`
- `src/ui/OptimizedImage.tsx`
- `src/ui/AvatarImage.tsx`
- `src/features/profile/ProfileHeaderCover.tsx`
- `src/ui/SegmentTabs.tsx`
- `src/ui/ProfileIconButton.tsx`

样式:

- `src/styles/features/post-detail.css`
- `src/styles/components/media.css`
- `src/styles/system/ui-primitives-lightbox.css`
- `src/styles/features/profile-shared-header.css`
- `src/styles/features/profile-modern.css`
- `src/styles/features/user-space-next.css`
- `src/styles/features/profile-avatar-action.css`

静态计数:

- 详情页/Profile/UserSpace/媒体相关 TSX utility-risk matches: 39。

说明:

- 本阶段没有重新运行浏览器截图采集，因此不声称完成动态视觉回归。
- 当前 Product Design saved context 不存在，本阶段基于项目源码、现有审计上下文和当前代码证据。

## 总体判断

媒体与身份系统整体比 Admin 更成熟，已经具备较好的语义类和 token 基础。

正向基础:

- Profile 与 UserSpace 共享 `profile-shared-header.css`，说明身份头部已经开始收敛。
- `ProfileHeaderCover` 是独立组件，封面图和默认封面都走同一入口。
- `AvatarImage` 统一处理头像 URL、失败 fallback、首字母兜底和优化图加载。
- `PostMediaGrid` 已统一 feed/detail 的媒体轮播、计数、拖动和 lightbox 入口。
- `ImageLightbox` 支持 Escape、方向键、滑动、预加载邻近图片和滚动锁。
- `post-detail.css` 已有完整 detail token，底部 action row 已经是 5 等分 grid。

主要风险:

- 媒体组件 TSX 中仍有 `h-full w-full object-cover object-center`、`relative`、`focus-visible:z-10` 等 utility，说明 Media Contract 还没有完全组件化。
- `post-detail.css` 仍在兼容 `.font-semibold`、`.font-bold`、`.text-xs`、`.flex-col` 等 utility，这是 CSS 强压信号。
- `SegmentTabs` 和 `ProfileIconButton` 仍直接拼接布局 utility，Profile tab 和 header avatar 尚未完全由组件 props 驱动。
- Profile/UserSpace 的名称区域使用横向滚动兜底，短期避免溢出，但不是最精致的身份信息策略。
- 头像 fallback 当前很有风格，但默认字是 `财`，在非金融/非中文场景下可能过强，需要产品语义确认。

## P0: Detail topbar 需要退出 utility 兼容层

证据:

`PostDetail.tsx` 中:

```text
detail-topbar-actions flex shrink-0 items-center gap-1.5
detail-topbar-author ... text-left
className="shrink-0"
detail-topbar-contact-button shrink-0
```

`post-detail.css` 中:

```text
.detail-topbar-author :is(.min-w-0, .flex-col)
.detail-topbar-author :is(.font-semibold, .font-bold)
.detail-topbar-author :is(.x-meta, .x-caption, .text-xs)
```

问题:

详情页 topbar 已经有 `detail-topbar-*` 语义类，但仍需要 CSS 去兜住旧 utility 或外部 utility。这会让未来 topbar 的像素由两套系统共同决定。

影响:

- 作者名、时间、关注按钮、联系按钮在窄屏下容易出现临界宽度漂移。
- CSS 选择器覆盖 utility 会扩散维护风险。
- 很难证明“不用 CSS 强压”。

建议:

新增或收敛为:

- `DetailTopbar`
- `DetailTopbarAuthor`
- `DetailTopbarActions`
- `DetailAuthorAvatar`
- `DetailTopbarActionSlot`

验收:

- `PostDetail.tsx` 不再给 topbar 拼 `flex/gap/shrink/text-left` utility。
- `post-detail.css` 不再需要兼容 `.font-semibold/.font-bold/.text-xs/.flex-col`。
- topbar 作者、关注、联系三个区域都由 detail semantic class 或 data attribute 控制。

## P0: Media Grid 需要从 utility 常量迁到 Media Contract

证据:

`PostMediaGrid.tsx` 中:

```text
MEDIA_IMAGE_CLASS = 'ui-media-image h-full w-full object-cover object-center'
MEDIA_TILE_BASE_CLASS = 'relative block overflow-hidden touch-manipulation ui-media-grid-item media-carousel-slide focus-visible:z-10'
media-carousel-slide-single h-full w-full
h-full min-h-0 w-full
```

当前 `media.css` 已经定义:

- `.ui-media-grid`
- `.ui-media-frame`
- `.ui-media-grid-item`
- `.media-carousel-slide`
- `.optimized-image`
- `.media-carousel-count`

问题:

Media 的主要视觉已经在 CSS 合同中，但 TSX 仍把尺寸、对象裁切、定位和 focus 层级写在常量 class 中。

影响:

- PostCard、PostDetail、上传预览、广告图后续很容易出现不同媒体半径和裁切方式。
- 单图、多图、lightbox 之间无法用一套语义说明 object-fit。
- 如果后续想做 Instagram 式媒体瀑布或比例策略，会继续改 TSX 常量。

建议:

将媒体状态改为 data contract:

```text
data-media-fit="cover | contain"
data-media-priority="true | false"
data-media-mode="feed | detail | upload | ad"
data-media-count="1 | n"
data-media-state="loading | loaded | error"
```

新增或收敛:

- `MediaFrame`
- `MediaCarousel`
- `MediaTile`
- `MediaImage`
- `MediaCounter`

验收:

- `PostMediaGrid.tsx` 不再包含 `h-full/w-full/object-cover/relative/focus-visible:z-*`。
- 图片裁切、比例、圆角、计数 badge 全部由 `media.css` 和 token 控制。
- Feed 和 Detail 可以通过 `data-media-mode` 调整密度，而不是另写页面覆盖。

## P0: Profile/UserSpace 身份头部方向正确，但名称策略还不够高级

证据:

`ProfileMobilePage.tsx` 和 `UserSpace.tsx` 共享:

- `profile-identity-section`
- `profile-identity-card`
- `profile-identity-main`
- `profile-avatar-stack`
- `profile-stats-row`
- `profile-identity-copy`

`profile-shared-header.css` 中:

```text
.profile-name-row {
  overflow-x: auto;
  scrollbar-width: none;
}
```

问题:

横向滚动能避免长名字撑破布局，但对身份头部来说不够精致。顶级社交产品通常会优先用层级、截断、二级 handle 或多行策略，而不是让姓名区域可以横向滚。

影响:

- 长昵称时用户很难意识到名字可以横向滑动。
- 头像、统计、名称之间的视觉关系在极窄屏可能不稳定。
- 精致感会被“隐藏滚动条的横向文字”削弱。

建议:

建立 Profile Identity Contract:

```text
displayName: primary, one-line ellipsis
handle: secondary, optional
stats: numeric compact, tabular
bio: max 3 lines, linkified
actions: one or two equal columns
```

验收:

- 长昵称只 ellipsis，不横向滚动。
- Profile 和 UserSpace 同一套身份头部，不再通过页面差异修补。
- 空 bio、长 bio、链接 bio、机器人用户 bio 都有明确显示规则。

## P0: SegmentTabs 仍由通用组件直接拼 utility

证据:

`SegmentTabs.tsx` 中:

```text
ui-segment-tabs grid w-full min-w-0 grid-cols-[repeat(...)] items-stretch
ui-segment-tab pressable flex flex-col items-center gap-0.5
min-w-0 max-w-full truncate
tabular-nums opacity-70
```

问题:

Profile tab 的视觉在 `profile-modern.css` 中已经有大量语义规则，但组件本身仍输出较多布局 utility。这个问题不只影响 Profile，以后任何使用 `SegmentTabs` 的页面都会继承这套混合合同。

建议:

让 `SegmentTabs` 输出更干净的结构:

```text
ui-segment-tabs
ui-segment-tab
ui-segment-tab-icon
ui-segment-tab-label
ui-segment-tab-meta
ui-segment-tab-indicator
```

并通过 props 表达:

```text
density="compact | regular"
labelDisplay="truncate | full"
variant="underline | pill | filter"
```

验收:

- `SegmentTabs.tsx` 不再拼 layout utility。
- Profile 的 underline tab 和其他 pill/filter tab 可以通过 variant 区分。
- meta 数字始终 tabular，长 label 只按组件 prop 决定截断或完整展示。

## P1: Lightbox 交互好，但视觉合同还要去 utility

证据:

`ImageLightbox.tsx` 中:

```text
lightbox-viewport relative z-10 h-full w-full overflow-hidden touch-none select-none
lightbox-slide relative h-full w-full flex items-center justify-center overflow-hidden px-0
lightbox-image block h-auto w-auto object-contain select-none
```

`ui-primitives-lightbox.css` 已有:

- `.ui-lightbox`
- `.ui-lightbox-control`
- `.lightbox-image`
- `.ui-lightbox-counter`

问题:

Lightbox 的交互完整度不错，但 viewport/slide/image 的核心布局还在 TSX utility。

建议:

补齐 CSS 合同:

- `.lightbox-viewport`
- `.lightbox-slide`
- `.lightbox-image`
- `.lightbox-stage`

验收:

- `ImageLightbox.tsx` 只输出语义类。
- close button、counter、image stage 的安全区和层级都由 token 管理。
- 横竖图、超长图、失败图、快速切图都有稳定布局。

## P1: Avatar fallback 需要语义分级

证据:

`AvatarImage.tsx`:

- URL 失败后使用 fallback initial。
- 无 initial 时显示 `财`。

`media.css`:

- `.ui-avatar-fallback` 使用深色金色渐变、发光、宋体 glyph。

判断:

这个 fallback 有辨识度，但视觉语气偏强。对于品牌头像、机器人、匿名用户、普通用户，可能不应该全部使用同一强烈金色 fallback。

建议:

增加头像 fallback tone:

```text
data-avatar-tone="user | anonymous | robot | brand | admin"
```

验收:

- 匿名用户 fallback 更低噪声。
- 普通用户 fallback 仍有质感但不抢内容。
- 机器人或品牌账号可保留更强识别。
- 默认 glyph 不依赖单个固定字，除非产品明确要求。

## P1: Detail 底部 action row 已接近合格，但需要组件合同

证据:

- `detail-bottom-actions-row` 和 `detail-bottom-actions-grid` 都是 repeat(5)。
- `detail-bottom-action` 已用 token 控制 icon、gap、padding、count max width。
- 联系入口缺失时用 hidden placeholder 保持五列稳定。

优点:

- 这是一个正确的布局策略，避免操作栏跳动。
- X/Threads 式低噪声 action row 已基本成型。

问题:

底部 action 的 class 拼接仍在 `PostDetail.tsx` 内完成，热度、点赞、分享、引用、联系各自声明状态。

建议:

抽 `DetailActionBar`:

```text
items: heat | like | share | quote | contact
layout: mobile | desktop
state: active | pending | disabled | empty
```

验收:

- `PostDetail.tsx` 只传动作语义和计数。
- 五列稳定性由组件内部保证。
- pending、liked、empty 的视觉状态不靠页面拼字符串。

## P2: Token 体系细节

需要继续 token 化或语义化的细节:

- `@media (max-width: 380px)` 和 `@media (max-width: 390px)` 应纳入 breakpoint token 或统一说明。
- `profile-modern-page { --ui-sticky-tab-top: 0px; }` 应确认是否需要 token，避免裸 `0px`。
- `media.css` 中 `filter: blur(0.2px)` 是极小视觉调味，建议 token 化或移除。
- `ProfileIconButton.tsx` 中 `!p-0` 是强压信号，应改为头像按钮 variant。
- `ProfileMobilePage.tsx` 安全/联系方式编辑区域仍有较多表单 utility，这属于 Profile settings contract，后续单独审。

## Benchmark 映射

Instagram:

- 媒体比例、圆角、计数、lightbox 必须保持统一。
- Profile 首屏应快速建立头像、名称、数据、简介、动作的身份层级。

X:

- 详情页 action row 应低噪声、稳定、可快速扫过。
- 作者 topbar 不应抢正文和媒体。

Threads:

- Profile 和详情页都应减少卡片厚重感，用线、留白、字体层级表达结构。
- 动效应短而轻，点赞反馈可以有，但不能成为主视觉。

Apple:

- 图片、头像、封面、lightbox 的行为必须可预期。
- 每个控件的布局所有权单一，不由 TSX utility 和 CSS override 共同决定。

## 动态验证门禁

后续要证明此阶段完成，必须补以下动态证据:

1. 详情页无图、有一图、多图、九图、超过九图。
2. 图片加载中、图片失败 fallback、lightbox 打开/关闭/左右切换。
3. 详情页匿名用户、普通用户、已关注、未关注、可联系、不可联系。
4. 底部 action count 超长时不挤掉图标。
5. Profile 空 bio、长 bio、链接 bio、长昵称。
6. UserSpace 自己、他人、可联系、不可联系、机器人用户。
7. 移动端 360/390/430 宽度和桌面宽度对比。
8. 键盘 Escape/ArrowLeft/ArrowRight 控制 lightbox。
9. prefers-reduced-motion 下点赞反馈和图片 fade 不造成多余运动。

建议命令:

```text
npm run test:goal-168-ui
npm run lint:ui-hardcoding
```

说明: 本阶段未运行这些命令；它们是后续验收门禁，不是本阶段完成证据。

## 结论

媒体与身份层级已经有较好的设计系统基础，但还没有完全达到“顶级产品像素合同”的程度。下一步的重点不是重设计，而是把已经存在的好方向收敛成稳定组件合同:

- Detail topbar 退出 utility 兼容。
- MediaGrid/Lightbox 退出 TSX 布局 utility。
- Profile/UserSpace 身份头部用明确名称和 bio 策略替代横向滚动兜底。
- SegmentTabs、ProfileIconButton、DetailActionBar 组件化，减少页面拼 class。

这会直接提升精致感和可维护性，也更符合“不硬编码、不临时方式、不 CSS 强压”的约束。

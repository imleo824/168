# UI/UE 深度审计 Phase 17 - 发布页状态摘要合同审计

日期: 2026-06-26

目标: 将发布页工具栏的图片、分类、地点、隐私、联系方式摘要从“能不能显示文字”的局部问题，提升为发布器状态摘要合同。此阶段聚焦 `/create`，覆盖创作器工具栏、图片上传入口、地点入口、隐私入口、联系方式入口、底部 sheet 和移动端窄屏稳定性。

## 本阶段读取证据

代码:

- `src/features/post-create/PostCreatePage.tsx`
- `src/features/upload/ImageUpload.tsx`
- `src/features/post-create/postCreateSheets.tsx`

样式:

- `src/styles/features/create-promote-post-editor.css`
- `src/styles/features/create-promote-post-details.css`
- `src/styles/features/create-promote-responsive.css`

既有审计上下文:

- `docs/review/ui-ux-audit-2026-06-26/phase-13-release-1-implementation-spec.audit`
- `docs/review/ui-ux-audit-2026-06-26/phase-15-benchmark-principle-mapping.audit`
- `docs/review/ui-ux-audit-2026-06-26/phase-16-admin-design-debt-audit.audit`

证据边界:

- 本阶段是静态合同审计，不声称完成了运行时截图回归。
- 当前 Product Design saved context 不存在，审计基于本项目源码、既有截图目录和当前代码证据。
- 发布页图片摘要已经过一次补丁修复，当前合同仍需要更系统的验收门禁。

## 总体判断

发布页摘要问题的根因不是某一个字段没有渲染，而是工具栏缺少明确的 Summary Contract。

当前已有正向基础:

- 五个工具入口已经有统一的 `.post-create-tool-row` 容器。
- 普通工具按钮有 `.post-create-tool-summary` 摘要样式。
- `ImageUpload` 已支持 `toolbarSummary`，且 memo 比较函数已包含 `toolbarSummary`。
- 隐私和联系方式 sheet 已有克制表达，不暴露具体 Telegram handle。
- 移动端输入稳定性已有 `.post-create-stable-focus`、`scroll-margin`、`overscroll-behavior` 等处理。

但仍有风险:

- 工具栏是 grid 五列，但按钮自身仍保留 `--post-create-tool-button-width` 宽度语义，布局所有权不够纯净。
- 图片按钮的摘要样式和普通工具按钮相似，但不是同一个组件合同。
- 分类结构化地点和独立地点在语义上必须分离，但需要在 UI 上让用户清楚“这个地点属于分类字段”还是“这是发布地点”。
- 联系方式工具栏只显示 `显示/隐藏` 是正确方向，但 sheet 内的 `展示/隐藏`、主按钮的 `显示/隐藏` 需要统一文案。
- 发布页状态摘要缺少一套静态和动态验收矩阵，导致问题容易反复。

## P0: 工具栏布局所有权必须单一

证据:

- `.post-create-tool-row` 使用 `grid-template-columns: repeat(var(--post-create-tool-button-count), minmax(0, 1fr))`。
- `.post-create-tool-button` 和 `.image-upload-toolbar-trigger` 仍使用 `width: var(--post-create-tool-button-width)`、`flex: 0 0 var(--post-create-tool-button-width)`。
- 这会让“容器五等分”和“按钮自算五分之一”同时存在。

问题:

视觉上可能刚好可用，但合同上不够干净。只要 gap、列数、响应式边界或工具数量变化，摘要区域就可能再次被挤压。

影响:

- 图片 `0/9`、`N/9` 这类短摘要也可能因为 flex 收缩、图标偏移或按钮宽度重复计算而不可见。
- 隐私、地点、联系方式等更长摘要更容易被省略。
- 后续想提升呼吸感时，开发者容易继续补局部 CSS，而不是修布局合同。

建议:

将工具栏合同收敛为:

- `post-create-tool-row` 只负责五列布局和 gap。
- 每个直接入口只负责内部图标和摘要排列。
- 图片预览区、错误提示区始终独占下一行。
- 按钮不再自己计算五分之一宽度。

验收:

- 五个入口在桌面和移动端始终同一行。
- 每个入口 `min-width: 0`，摘要只 ellipsis，不挤掉图标。
- 工具数量由 `--post-create-tool-button-count` 控制，不需要按钮二次计算宽度。

## P0: 图片摘要必须是稳定状态，而不是可选装饰

当前状态:

- 发布页传入 `toolbarSummary`。
- `ImageUpload` 内部优先显示上传中，其次显示 `toolbarSummary`，再 fallback 到 `showToolbarImageCount`。
- `ImageUpload` memo 比较函数包含 `toolbarSummary`。
- 当前图片摘要应稳定为 `0/9`、`N/9`。

问题:

图片入口承担的是“媒体容量状态”，不是普通 hint。对用户来说，`0/9` 是是否还能继续添加图片的主要反馈之一。

建议:

建立图片按钮状态表:

| 状态 | 摘要 | 视觉权重 |
| --- | --- | --- |
| 无图 | `0/9` | 低噪声，但必须可见 |
| 上传中 | `上传中` | 高于计数 |
| 已完成 | `N/9` | 稳定可见 |
| 达上限 | `9/9` | 可见，入口 disabled 或显示上限原因 |
| 引用发布禁用 | `0/9` 或不强调计数 | 必须由 title/disabled reason 解释 |

验收:

- 添加、删除、上传失败、重新选择图片时摘要即时变化。
- 摘要变化不依赖其他表单状态触发重渲染。
- 图片按钮的视觉状态和提交 payload 中 `images` 数量一致。

## P0: 地点摘要和分类结构化地点必须分层

证据:

- 独立地点使用 `form.location` 和 `locationDisplayLabel`。
- 分类结构化地点来自 `categoryDraft.categoryMeta[...]`。
- 提交时存在 `activeCategoryLocationValue || form.location` 的业务语义。

问题:

UI 摘要不能简单把分类结构化地点冒充为独立地点按钮。用户会误以为点击地点按钮能编辑该字段，但实际入口可能在分类信息里。

建议:

保持两层摘要:

- 独立地点按钮只显示 `locationDisplayLabel`。
- 分类结构化地点在分类摘要或分类编辑入口里表达。
- 提交 payload 可以继续按业务优先级合成，但 UI 必须让来源清楚。

验收:

- 只选独立地点: 地点按钮显示城市名。
- 只填分类结构化地点: 分类信息内可见，独立地点按钮不伪装显示。
- 两者同时存在: UI 明确两者来源，提交逻辑有单独注释或测试覆盖。

## P0: 隐私和联系方式摘要应克制且一致

当前方向:

- 隐私摘要使用 `公开/匿名`。
- 联系方式摘要使用 `显示/隐藏`。
- 联系方式不在工具栏暴露具体 `@handle`。

这是正确方向。原因是发布工具栏是轻量状态区，不是账户资料展示区。

需要统一:

- 工具栏联系方式使用 `显示/隐藏`。
- sheet 内目前有 `展示/隐藏` 表达，应统一为 `显示/隐藏` 或统一反向为 `展示/隐藏`，避免一个功能两套文案。
- privacy sheet 内 `公开发布` 与工具栏 `公开/匿名` 语义清楚，但切换后的反馈要即时同步。

验收:

- 切换匿名后，工具栏立即从 `公开` 变 `匿名`。
- 切换联系按钮后，工具栏立即从 `显示` 变 `隐藏`。
- 清空 Telegram 时不在工具栏显示用户名，也不展示泄露性错误文本。

## P1: Sheet 和工具栏需要共享字段语气

证据:

- `PostCreatePrivacySettingsSheet`、`PostCreateTelegramSettingsSheet` 使用 `BottomSheet`。
- `PostCreateContactSettingsContent` 使用 `post-create-settings-row`。
- 联系方式编辑使用 `ProfileDialog` 和 `post-create-contact-input-wrap`。

问题:

工具栏是轻入口，sheet 是编辑层，两者目前样式语义大体一致，但还没有命名层面的合同。例如设置项、follow-up row、错误/提示文案都分散在页面功能类中。

建议:

抽象发布设置合同:

- `PostCreateSettingRow`
- `PostCreateSettingValue`
- `PostCreateSettingSwitch`
- `PostCreateInlineHint`
- `PostCreateInlineError`

验收:

- privacy、telegram、contact editor 共用同一套设置行和 hint/error 语气。
- 不通过临时 margin 或页面专属 override 修正 sheet 间距。
- sheet 内文案和工具栏摘要用同一份状态映射。

## P1: 与竞品原则的映射

Threads:

- Compose 工具栏应该安静、轻、稳定，不像后台表单。
- 状态摘要应帮助用户确认选择，而不是制造视觉噪声。

X:

- 发布入口的媒体计数、隐私状态、可联系状态要可快速扫过。
- 工具栏不应因为状态变化造成跳动。

Instagram:

- 图片入口是媒体发布的核心反馈，计数要清晰。
- 媒体预览和工具入口应分层，不互相挤压。

Apple:

- 状态应该清楚、可预期、直接反映系统当前值。
- 控件职责要单一，布局和内容不互相夺权。

## P1: 建议的发布页 Summary Contract

建议新增一个轻量状态映射，而不是散落 `useMemo`:

```text
PostCreateToolSummary = {
  image: {
    label: "0/9" | "N/9" | "上传中",
    state: "idle" | "active" | "busy" | "disabled"
  },
  category: {
    label: categoryName,
    state: "idle" | "active" | "invalid"
  },
  location: {
    label: locationDisplayLabel,
    state: "idle" | "active"
  },
  privacy: {
    label: "公开" | "匿名",
    state: "public" | "anonymous"
  },
  telegram: {
    label: "显示" | "隐藏",
    state: "visible" | "hidden" | "invalid"
  }
}
```

要求:

- 这只是状态映射，不引入额外复杂 UI 状态。
- label 由当前表单值派生。
- state 用于 data attribute 和视觉状态，不直接拼颜色。

## P2: 字体、颜色、间距细节

字体:

- 工具摘要使用 `--ui-text-xs` 是合理的。
- 摘要 weight 应保持 strong，但不应比图标和正文更抢。
- 地点长文本必须 ellipsis，不换行。

颜色:

- 默认摘要沿用 currentColor 是正确方向。
- active 状态不应让整块按钮过度品牌化。
- 错误态只用于真实错误，不用于未填写。

间距:

- 图标和摘要之间应由统一 gap/token 管理。
- 图片按钮不应单独靠 transform 微调来维持视觉平衡，除非该 offset 已被明确 token 化并有注释说明。
- 预览区和错误提示区必须从五列工具栏布局中分离。

## 动态验证门禁

发布页摘要完成不能只看代码。后续 Release 必须补以下动态证据:

1. 空状态: 五个工具入口同一行，图片显示 `0/9`，隐私和联系方式摘要可见。
2. 添加一张图片: 图片摘要立即变 `1/9`。
3. 添加多张图片: 图片摘要显示 `N/9`，预览区独占下一行。
4. 删除图片: 图片摘要回退，直到 `0/9`。
5. 上传中: 摘要显示 `上传中`，提交按钮说明一致。
6. 独立地点: 地点按钮显示城市名。
7. 分类结构化地点: 分类入口展示，不冒充独立地点按钮。
8. 隐私切换: `公开/匿名` 即时同步。
9. 联系方式切换: `显示/隐藏` 即时同步。
10. 窄屏: 图标不被挤掉，摘要只省略文字，不重叠、不换行。

建议命令:

```text
npm run test:goal-168-ui
npm run lint:ui-hardcoding
```

说明: 本阶段未运行这些命令；它们是后续验收门禁，不是本阶段完成证据。

## 结论

发布页摘要已经从一次具体故障暴露出更大的设计系统问题: 工具栏缺少完整 Summary Contract。

下一步不应该继续给每个按钮补独立 CSS，而应该把“状态如何派生、摘要如何显示、布局谁负责、sheet 如何同步”收敛成发布页自己的小型合同。这样才符合精致感、呼吸感、简约感，也符合“不硬编码、不临时方式、不 CSS 强压”的约束。

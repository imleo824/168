# UI/UE 深度审计 Phase 4 - 顶层 UI 组件像素级审计

日期: 2026-06-26

目标: 深读剩余高杠杆 UI 组件，继续寻找影响全站精致感、呼吸感、简约感的系统性问题。重点不是指责单个组件，而是判断组件合同是否足够稳定、语义是否足够清晰、是否存在硬编码尺寸或 CSS 强压倾向。

## 本阶段已深读组件

1. `AvatarImage`
2. `IconButton`
3. `TopbarIconButton`
4. `BottomSheet`
5. `ImageLightbox`
6. `OptimizedImage`
7. `LinkifiedText`
8. `TopbarActions`
9. `ErrorBoundary`
10. `ListLoadMoreState`
11. `PageLoader`
12. `HeaderSelectAction`
13. `ProfileIconButton`
14. `PublishIconButton`
15. `SettingRow`
16. `TelegramContactIconButton`
17. `TopbarBackIcon`
18. `Skeleton`

## 本阶段已深读样式合同

1. `ui-primitives-lightbox.css`
2. `telegram-contact-action.css`
3. `ui-skeleton-contract.css`
4. `ui-control-shape-contract.css`

## 总体判断

- 组件功能完整度高于视觉合同完整度。
- 多数组件都有正确的可访问性意识，例如 `aria-label`、`role`、`aria-live`、键盘 Escape、图片 fallback、错误恢复。
- 最大设计债不是“缺样式”，而是 action、icon、media、sheet、skeleton 几个家族没有完全统一到同一套 size、stroke、density、surface、motion token。
- 当前很多局部数值是合理工程默认值，但从顶级 UI 视角看，它们会让全站出现细小漂移，最终损伤精致感。

## P0 组件家族问题

1. Icon action 家族分裂
涉及组件: `IconButton`、`TopbarIconButton`、`PublishIconButton`、`ProfileIconButton`、`TelegramContactIconButton`、`HeaderSelectAction`、`SettingRow`、`BottomSheet`、`ImageLightbox`。
证据: 多个组件各自决定图标尺寸、stroke、圆角、背景、类名。
具体表现: `PublishIconButton` 内部返回 `19`、`24`、`2.25`、`2.8` 这类数值；`BottomSheet` close icon 使用 `17`；`ImageLightbox` close icon 使用 `18`；`HeaderSelectAction` caret 使用 `12`；`SettingRow` chevron 使用 `16`；`ProfileIconButton` 内含 `!p-0`。
影响: 同样是图标按钮，视觉重量和触控节奏会微妙不一致，页面越多越明显。
建议: 建立统一 `IconAction` 合同。
建议 API: `size="xs|sm|md|lg"`、`tone="quiet|muted|brand|danger|inverse"`、`shape="control|pill|circle"`、`context="topbar|sheet|lightbox|feed|profile"`。
落地原则: 所有 icon size 和 stroke width 走 token，禁止组件内返回具体数字。

2. 状态和加载家族仍有重复边界
涉及组件: `ErrorBoundary`、`ListLoadMoreState`、`PageLoader`、`Skeleton`、此前已读的 `UIStateBlock`、`EmptyStateCard`、`LoadingState`。
证据: `ErrorBoundary` 使用 `StateBlock`，这是好方向；`ListLoadMoreState` 独立拼加载更多；`PageLoader` 自己拼卡片 skeleton；`Skeleton` 又有多套页面 skeleton。
具体表现: `PageLoader` 使用 `p-3.5`、`width={36}`、`height={36}`、`height={136}`、`width={112}`、`width={160}` 等具体尺寸；`ListLoadMoreState` 的 spinner 传 `16`。
影响: 加载态和真实内容如果不是同一套尺寸合同，切换时会有跳动感，也会显得不够 Apple 式冷静。
建议: 建立 `SkeletonPreset` 和 `LoadingStatePlacement`。
建议 API: `preset="feed|detail|profile|record|payment"`、`density="compact|regular|spacious"`、`placement="page|feed|panel|inline"`。
落地原则: `PageLoader` 不直接写 skeleton 尺寸，改为调用 preset。

3. Surface depth 在弹层、空态、卡片之间不统一
涉及组件: `BottomSheet`、`ImageLightbox`、`ErrorBoundary`、`Skeleton`、`SurfaceSectionCard`。
证据: lightbox close control 使用独立白色高亮和阴影，bottom sheet 接受任意 `panelClassName`，ErrorBoundary 包一层 `ui-error-boundary`，Skeleton 内仍出现 `surface-card`。
影响: 页面层、浮层、图片层的材料感不稳定，阴影和白卡片会在不同场景里抢戏。
建议: 建立 `SurfaceDepth` token。
建议 depth: `flat`、`hairline`、`raised`、`floating`、`modal`、`immersive`。
落地原则: lightbox 属于 `immersive`，sheet 属于 `modal`，普通页面状态属于 `hairline` 或 `flat`。

## P1 组件问题

1. `AvatarImage`
优点: 输入清洗、异常图片 fallback、匿名名处理都比较稳。
风险: fallback glyph 默认是 `财`，品牌感强但可能不适合所有用户身份场景；fallback 的 `aria-label` 使用 `avatar-*` 英文拼接，和中文产品语境不完全一致。
风险: 组件通过 `mergeClassName` 注入 `ui-avatar`，尺寸仍依赖外部 className，不够语义化。
建议: 增加 `size="xs|sm|md|lg|profile"` 和 `fallbackTone="brand|neutral|anonymous"`。
建议: fallback aria 使用中文语义，例如“用户头像占位”或传入可本地化 label。

2. `IconButton`
优点: 小而干净，variant 简洁。
风险: 只有 `quiet|action`，无法覆盖 topbar、sheet、lightbox、danger、brand 等真实场景，导致其他组件继续自建按钮。
建议: 作为 IconAction 基础层扩展，而不是让 `TopbarIconButton`、`PublishIconButton` 各自漂移。

3. `TopbarIconButton`
优点: 已有 `tone` 和 `useInstantPress`，交互方向正确。
风险: 通过 `className` 是否包含 `ui-topbar-back-button` 判断是否渲染 back icon，这是隐性字符串契约。
风险: TSX 中直接拼 `inline-flex items-center justify-center rounded-full transition`，和“不硬编码、不强压”的目标不完全一致。
建议: 增加 `action="back|generic"` 或由调用方显式传 `kind`，不要通过 className 推断。

4. `PublishIconButton`
优点: 发布入口独立封装，语义重要。
风险: icon size 和 stroke width 以函数返回具体数字；variant `top` 和 `topbar` 实际同义，容易让调用方困惑。
建议: 合并 `top|topbar`，新增 token 类控制 icon 尺寸和 stroke。
建议: 发布按钮归入统一 IconAction，但保留 `intent="publish"` 的品牌语义。

5. `ProfileIconButton`
优点: 头像入口和 `AvatarImage` 连接直接。
风险: className 内有 `!p-0`，这是明显 CSS 强压信号。
建议: 建立 `ui-avatar-action` 的 padding/size 合同，移除 `!` 类。

6. `TelegramContactIconButton`
优点: 样式合同比较干净，CSS 已用 token 控制尺寸、颜色、hover。
风险: 它是当前 icon action 家族里较好的样本，但仍独立于 `IconButton`。
建议: 把它作为 IconAction token 化样板，后续其他 icon action 向它靠拢。

7. `HeaderSelectAction`
优点: 组件简单，和 topbar compact action 有连接。
风险: caret size 直接写 `12`，select 视觉高度完全依赖 className。
建议: caret 尺寸走 `--ui-topbar-select-caret-size`，select density 走 token。

8. `SettingRow`
优点: 信息结构清楚，支持 icon、value、trailing、chevron。
风险: Chevron size 直接写 `16`；使用 `aria-expanded` 表达 active，但如果不是展开控件语义可能不准确。
建议: `active` 拆为 `selected` 和 `expanded`，避免语义混用。
建议: row density、icon size、chevron size 全部 token 化。

9. `TopbarActions`
优点: action group、count badge、online badge 独立，是正确抽象。
风险: `TopbarActionGroup` 使用 `div aria-label`，没有 role 时 label 对部分辅助技术意义有限。
建议: 如果只是布局容器不需要 aria-label；如果是动作组，可考虑 `role="group"`。

10. `TopbarBackIcon`
优点: 保持极简。
风险: ChevronLeft 方向固定。若未来支持 RTL 或国际化，需要方向 token 或自动翻转。
建议: 暂时低优先级。

## P1 媒体组件问题

1. `OptimizedImage`
优点: 支持优化 URL、srcSet、fallback、priority、fade 状态、full image preserve，工程质量不错。
风险: `defaultSizesForVariant` 内含 `96px`、`180px`、`860px`、`420px`，和设计 token 分离。
风险: `FALLBACK_IMG` 是透明 1px 图，坏图可能在视觉上静默消失。
建议: 建立 media size preset token。
建议 preset: `avatarThumb`、`feedMedia`、`detailMedia`、`lightbox`。
建议: fallback 状态可以允许传 `fallbackVisual="blank|placeholder|hidden"`，默认对内容图片展示轻量占位。

2. `ImageLightbox`
优点: 已有 Escape、左右方向键、手势滑动、预加载、route change close，交互基础很扎实。
风险: 手势阈值 `44`、`1.1`、`8` 是行为常量，可以保留，但应命名为 gesture token/constant。
风险: transition 写在 style 内: `transform 140ms cubic-bezier(0.2, 0, 0.2, 1)`，没有复用 motion token。
风险: 结构 className 含 `absolute z-30`、`relative z-10`、`h-full w-full`、`px-0` 等 utility。
风险: root focus 存在，但没有完整 focus trap。截图审计无法证明键盘可达性完整。
建议: lightbox motion、layer、viewport、slide 类全部收敛到 CSS 合同。
建议: close control 使用 `IconAction context="lightbox"`，不要独立白色 shadow 方案。

3. `LinkifiedText`
优点: URL 识别和尾标点处理稳健，链接外跳安全属性正确。
风险: 默认 `linkClassName='profile-bio-link'` 让组件偏 profile 语义，复用到其他内容时会泄漏样式。
建议: 默认改为通用 `ui-linkified-link`，profile 只作为外部覆盖。

## P1 浮层组件问题

1. `BottomSheet`
优点: 支持 scroll lock、Escape、overlay close、文本输入释放，交互考虑较完整。
风险: `role="dialog"` 放在 overlay 容器，panel 没有 `aria-labelledby` 连接标题。
风险: `showHandle` 使用 `mx-auto mb-3`，close icon `X size={17}`，属于局部 utility/数字。
风险: `panelClassName`、`headerClassName`、`titleClassName`、`bodyClassName`、`closeClassName` 全部开放，强大但也容易绕开设计系统。
建议: 增加 `density`、`size`、`surfaceDepth`、`titleBehavior` 等显式 prop。
建议: 默认 panel/header/body/close 都用语义类，className 只做布局补充，不承载核心视觉。

2. `ErrorBoundary`
优点: chunk load 自动恢复逻辑优秀，默认 fallback 使用 `StateBlock`，方向正确。
风险: 当前错误态还没有和首页失败态、空态、登录态统一 placement。
建议: `ErrorBoundary` fallback 使用 `UIStateBlock placement="page"`，与 Phase 4 P0 状态体系重构合并。

## P2 骨架屏问题

1. `Skeleton`
优点: 已经有多套页面级 skeleton，说明团队意识到加载体验的重要性。
风险: 基础 `Skeleton` 允许任意 `width`、`height`，这是必要 escape hatch，但被 `PageLoader` 大量直接传数字后会形成硬编码习惯。
风险: `HomePageSkeleton`、`UserSpaceSkeleton`、`DetailPageSkeleton` 已经接近真实结构，但需要与真实卡片尺寸回归校准，否则会产生加载跳动。
建议: 保留基础 `Skeleton`，但页面级只能使用 preset class，不直接传具体数字。
建议: 对 feed、detail、profile、record、payment 建立 skeleton/real content 对齐检查。

2. `ui-skeleton-contract.css`
优点: 已是 facade，分拆为 primitives、home、feed、detail、recharge，架构方向正确。
风险: 仅有 facade 不够，TSX 侧仍能绕开 token 传内联尺寸。
建议: 增加 lint 或组件约定，页面 skeleton 不传裸数字。

## 样式合同观察

1. `ui-control-shape-contract.css`
优点: 这是非常重要的全局 control 合同，已经避免了很多页面局部 patch。
风险: 选择器覆盖面非常广，后续新增组件时容易无意被全局圆角规则影响。
建议: 保持它作为兜底，但核心组件应显式声明自己的 action/surface context，不只依赖全局兜底。

2. `ui-primitives-lightbox.css`
优点: lightbox 层级、背景、counter 已经 token 化。
风险: close control 的白色玻璃感和 shadow 较强，和“简约克制”目标略冲突。
建议: lightbox 关闭按钮降低阴影，转为更 Apple 式材料按钮，hover/active 不放大视觉噪声。

3. `telegram-contact-action.css`
优点: 这是本轮最符合目标的组件样式之一。尺寸、图标、颜色、hover 都有 token，视觉克制。
建议: 把它提升为 icon action family 的样板。

## Phase 5 推荐实施切口

1. P0: 建立统一 IconAction 组件
覆盖: `IconButton`、`TopbarIconButton`、`PublishIconButton`、`ProfileIconButton`、`TelegramContactIconButton`、sheet close、lightbox close、select caret、setting chevron。
收益: 全站图标按钮的尺寸、stroke、触控面积、圆角、hover 统一。
约束: 不在组件里写裸数字，不用 `!p-0`。

2. P0: 状态和加载 placement 合并
覆盖: `ErrorBoundary`、`ListLoadMoreState`、`PageLoader`、`UIStateBlock`、`EmptyStateCard`、`AuthRequiredState`。
收益: 首页错误、404、空列表、登录墙、加载更多会像同一套产品，而不是各自为政。
约束: page/feed/panel/inline 用 prop 表达，不用页面级 CSS 强压。

3. P1: 媒体 preset token 化
覆盖: `OptimizedImage`、`AvatarImage`、`ImageLightbox`、feed media、detail media。
收益: 图片尺寸、fallback、lightbox 体验统一，减少透明坏图和跳动。
约束: responsive sizes 由 preset 表达，不散落 px。

4. P1: BottomSheet 语义化
覆盖: sheet title、handle、close、body、footer、surface depth。
收益: 筛选、选择、确认、编辑类 sheet 视觉稳定。
约束: 少用开放 className 承载核心视觉。

5. P2: Skeleton 与真实内容校准
覆盖: 首页 feed、详情页、用户空间、充值、记录。
收益: 加载切换更稳，减少视觉跳动。
约束: 页面级 skeleton 不传裸尺寸。

## 本阶段结论

组件层已经具备很好的工程基础，但顶级精致感需要再往上抽一层统一合同。下一轮如果进入实施，最建议先做 `IconAction` 和 `StatePlacement`，因为它们覆盖页面最多、收益最高，也最符合“不硬编码、不临时、不 CSS 强压”的约束。
